[
  {
    "questionText": "Quelle est la <strong>bibliothèque de test recommandée</strong> par l'équipe React pour tester les composants React ?",
    "options": [
      "Enzyme",
      "React Testing Library",
      "Mocha",
      "Jasmine"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "React Testing Library est la bibliothèque officiellement recommandée. Elle encourage les tests basés sur le comportement utilisateur plutôt que sur les détails d'implémentation, contrairement à Enzyme (qui n'est plus maintenue activement)."
  },
  {
    "questionText": "Quelle commande installe <strong>Jest et React Testing Library</strong> dans un projet React créé avec Vite ?",
    "options": [
      "`npm install jest`",
      "`npm install @testing-library/react @testing-library/jest-dom vitest`",
      "`npm install react-test-renderer`",
      "`npm install enzyme enzyme-adapter-react-18`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Avec Vite, on utilise souvent Vitest (compatible Jest). L'installation typique : `@testing-library/react` pour le rendu des composants, `@testing-library/jest-dom` pour les matchers DOM supplémentaires, et `vitest` comme moteur de test."
  },
  {
    "questionText": "Dans React Testing Library, que fait la fonction <code>render()</code> ?",
    "options": [
      "Elle compile le composant en HTML statique",
      "Elle monte le composant dans un DOM virtuel (jsdom) et retourne des utilitaires pour interagir avec",
      "Elle exécute tous les tests du composant",
      "Elle génère une couverture de code"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`render()` crée un conteneur DOM virtuel (via jsdom) où le composant est monté. Elle retourne des méthodes comme `getByText`, `queryByRole`, `container`, etc., permettant d'interagir avec le composant rendu."
  },
  {
    "questionText": "Quelle est la <strong>bonne pratique</strong> pour sélectionner un élément dans un test avec React Testing Library ?",
    "options": [
      "Utiliser `data-testid` comme dernier recours, mais privilégier les rôles ARIA, le texte ou les labels",
      "Toujours utiliser `data-testid` car c'est le plus fiable",
      "Utiliser les classes CSS (`className`)",
      "Utiliser les IDs HTML des éléments"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "React Testing Library encourage les tests basés sur l'accessibilité. On privilégie `getByRole`, `getByText`, `getByLabelText`. `data-testid` n'est utilisé qu'en dernier recours quand aucun sélecteur sémantique n'est possible."
  },
  {
    "questionText": "Quelle est la méthode <strong>recommandée</strong> pour déclencher un clic sur un bouton dans un test ?",
    "options": [
      "`button.click()`",
      "`fireEvent.click(button)` de `@testing-library/react`",
      "`userEvent.click(button)` de `@testing-library/user-event`",
      "`button.simulate('click')`"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "`@testing-library/user-event` est la bibliothèque recommandée pour simuler les interactions utilisateur réelles (clics, saisies, focus). `fireEvent` est plus bas niveau et ne déclenche pas tous les événements qu'un vrai utilisateur générerait."
  },
  {
    "questionText": "Comment tester un composant qui affiche <strong>\"Chargement...\" puis des données après un appel API</strong> ?",
    "options": [
      "Utiliser `waitFor` ou `findBy*` pour attendre l'apparition des données",
      "Ajouter un `setTimeout` manuel dans le test",
      "Tester uniquement l'état de chargement",
      "Ne pas tester les composants asynchrones"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "`waitFor` permet d'attendre qu'une condition devienne vraie. Les queries `findBy*` (ex: `findByText`) sont aussi asynchrones et attendent que l'élément apparaisse. C'est la méthode idiomatique pour tester les comportements asynchrones."
  },
  {
    "questionText": "Quelle assertion vérifie qu'un élément <strong>n'est PAS présent</strong> dans le DOM ?",
    "options": [
      "`expect(element).not.toBeInTheDocument()`",
      "`expect(element).toBeNull()`",
      "`expect(element).toBeUndefined()`",
      "`expect(element).toBeVisible()`"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "`toBeInTheDocument()` est un matcher de `@testing-library/jest-dom`. Pour vérifier l'absence, on utilise `.not.toBeInTheDocument()`. Exemple : `expect(screen.queryByText('Absent')).not.toBeInTheDocument()`."
  },
  {
    "questionText": "Pour tester un champ de saisie, quelle approche est <strong>correcte</strong> ?",
    "options": [
      "`fireEvent.change(input, { target: { value: 'test' } })` puis vérifier `input.value`",
      "Utiliser `userEvent.type(input, 'test')` qui simule les vraies frappes",
      "Modifier directement `input.value = 'test'`",
      "Les deux premières sont correctes, mais `userEvent` est préféré"
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "`userEvent.type` simule les événements réels (keydown, keyup, change, input) et est plus proche du comportement utilisateur. `fireEvent` fonctionne mais est moins complet. La modification directe de `value` est une mauvaise pratique."
  },
  {
    "questionText": "Quelle est l'utilité de la fonction <code>cleanup()</code> dans React Testing Library ?",
    "options": [
      "Elle nettoie les variables globales après chaque test",
      "Elle démonte automatiquement les composants après chaque test pour éviter les interférences entre tests",
      "Elle supprime les fichiers temporaires",
      "Elle vide le cache de Jest"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`cleanup()` est appelée automatiquement après chaque test (via `afterEach`). Elle démonte les composants montés par `render()` et nettoie le DOM, garantissant que les tests sont isolés les uns des autres."
  },
  {
    "questionText": "Le code suivant teste un composant avec un appel API. Quelle est l'<strong>erreur</strong> ?\n```jsx\ntest('affiche les données', async () =&gt; {\n  render(&lt;MonComposant /&gt;);\n  expect(screen.getByText('Chargement...')).toBeInTheDocument();\n  // l'appel API est simulé\n  const data = await screen.findByText('Donnée 1');\n  expect(data).toBeInTheDocument();\n});\n```",
    "options": [
      "Il manque `waitFor` autour de `findByText`",
      "`findByText` retourne un élément, pas besoin de `await`",
      "Aucune erreur, ce test est correct",
      "Il faut utiliser `getByText` au lieu de `findByText`"
    ],
    "correctAnswer": 2,
    "difficulty": "hard",
    "explanation": "Ce test est correct : `getByText` vérifie immédiatement la présence du texte de chargement. `findByText` retourne une Promesse (d'où le `await`) et attend l'apparition des données après résolution de l'appel API simulé. La syntaxe est bonne."
  },
  {
    "questionText": "Comment <strong>simuler un module</strong> (ex: une API fetch) dans un test Jest ?",
    "options": [
      "`jest.fn()`",
      "`jest.spyOn()`",
      "`jest.mock('./monModule')`",
      "Toutes ces réponses sont correctes selon le contexte"
    ],
    "correctAnswer": 3,
    "difficulty": "hard",
    "explanation": "`jest.mock()` remplace tout un module. `jest.spyOn()` espionne une méthode existante. `jest.fn()` crée une fonction mock. Les trois sont utilisés : mock de module pour `fetch`, spyOn pour des méthodes d'objet, fn pour des callbacks."
  },
  {
    "questionText": "Quel outil permet de <strong>mesurer la couverture de code</strong> des tests dans un projet React ?",
    "options": [
      "React DevTools",
      "La commande `npm test -- --coverage` avec Jest",
      "ESLint",
      "Prettier"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Jest intègre Istanbul. La commande `npm test -- --coverage` (ou `vitest --coverage`) génère un rapport montrant le pourcentage de lignes, fonctions, branches et statements couverts par les tests."
  }
]