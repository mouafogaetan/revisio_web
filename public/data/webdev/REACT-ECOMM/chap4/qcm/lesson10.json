[
  {
    "questionText": "Quel est l'avantage principal de Zustand par rapport à Redux pour un projet e-commerce de taille moyenne ?",
    "options": [
      "Zustand est plus lent mais plus sécurisé",
      "Zustand a une courbe d'apprentissage plus douce et moins de code boilerplate",
      "Zustand nécessite obligatoirement Redux Toolkit",
      "Zustand ne fonctionne qu'avec TypeScript"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Zustand est une bibliothèque de gestion d'état légère qui réduit considérablement le code boilerplate comparé à Redux. Pas besoin d'actions, reducers, dispatch manuels. L'API est simple : un hook `create()` qui retourne un store avec des setters directs."
  },
  {
    "questionText": "Dans Zustand, comment créer un store authentification contenant un utilisateur et une fonction login ?",
    "options": [
      "`const useAuthStore = createStore({ user: null, login: () =&gt; {} })`",
      "`const useAuthStore = create((set) =&gt; ({ user: null, login: (userData) =&gt; set({ user: userData }) }))`",
      "`const useAuthStore = new Store('auth', { user: null })`",
      "`const useAuthStore = defineStore('auth', { state: () =&gt; ({ user: null }) })`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Avec Zustand, on utilise `create` qui reçoit une fonction `set` pour mettre à jour l'état. La syntaxe Redux-like n'existe pas ; le troisième exemple est du Pinia (Vue), le quatrième du Redux Toolkit. La bonne forme est : `create((set) =&gt; ({ ...state, ...actions }))`."
  },
  {
    "questionText": "Quel middleware Zustand permet de sauvegarder automatiquement le store dans localStorage ?",
    "options": [
      "`redux`",
      "`devtools`",
      "`persist`",
      "`immer`"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Le middleware `persist` (de `zustand/middleware`) permet de persister automatiquement l'état du store dans localStorage (ou sessionStorage, AsyncStorage). Exemple : `create(persist(myStore, { name: 'auth-storage' }))`. `devtools` est pour le debugging Redux, `immer` pour les mutations immutables, `redux` n'est pas un middleware Zustand natif."
  },
  {
    "questionText": "Observez ce code Zustand : &lt;br/&gt;<code>const useAuthStore = create((set) =&gt; ({ user: null, token: null, login: (user, token) =&gt; set({ user, token }) }));</code>&lt;br/&gt;Que se passe-t-il si on appelle `login` avec des données ?",
    "options": [
      "Les propriétés user et token sont remplacées, les autres propriétés restent inchangées",
      "Tout l'état est réinitialisé",
      "Une erreur est levée car set ne supporte pas les objets partiels",
      "Le store devient immuable"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "`set` fusionne l'objet partiel avec l'état actuel. Donc `set({ user, token })` met à jour uniquement `user` et `token` sans toucher aux autres propriétés du store. C'est un comportement similaire à `setState` de React."
  },
  {
    "questionText": "Pour éviter les re-rendus inutiles d'un composant qui utilise seulement `logout` depuis le store Zustand, quelle est la bonne pratique ?",
    "options": [
      "Utiliser `React.memo` sur chaque composant",
      "Sélectionner uniquement la fonction `logout` avec `useAuthStore((state) =&gt; state.logout)`",
      "Placer tout le store dans un useEffect",
      "Désactiver les re-rendus avec `useRef`"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Zustand permet de sélectionner des parties spécifiques du store via un sélecteur. Si on écrit `const logout = useAuthStore((state) =&gt; state.logout)`, le composant ne se re-rendra que si `logout` change (ce qui n'arrive jamais). En revanche, `const { logout, user } = useAuthStore()` provoquerait un re-rendu à chaque modification de `user` même si on utilise uniquement `logout`. C'est un point clé pour optimiser les performances."
  },
  {
    "questionText": "Comment réinitialiser complètement un store Zustand à son état initial (exemple : déconnexion) ?",
    "options": [
      "`useAuthStore.reset()`",
      "`useAuthStore.setState(initialState, true)`",
      "`useAuthStore.destroy()`",
      "C'est impossible, il faut recharger la page"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Zustand expose `setState` sur le store. On peut réinitialiser en appelant `useAuthStore.setState(initialState, true)`. Le second paramètre `true` indique de remplacer complètement l'état au lieu de fusionner. Une autre approche est de définir une action `reset` dans le store qui appelle `set(initialState)`."
  },
  {
    "questionText": "Avec le middleware `persist`, comment exclure une propriété sensible (ex: `password`) de la persistance dans localStorage ?",
    "options": [
      "C'est impossible, tout le store est persisté",
      "Ajouter `partialize: (state) =&gt; ({ user: state.user, token: state.token })` dans la configuration persist",
      "Utiliser `sessionStorage` au lieu de `localStorage`",
      "Crypter manuellement les données avant persist"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Le middleware `persist` accepte une option `partialize` qui est une fonction filtrant les données à sauvegarder. Exemple : `persist(store, { name: 'auth', partialize: (state) =&gt; ({ user: state.user, token: state.token }) })`. Cela évite de stocker des informations sensibles comme le mot de passe (qui ne devrait de toute façon pas être dans le store client)."
  },
  {
    "questionText": "Dans le cadre d'une application e-commerce, où placer typiquement la logique de refresh token automatique ?",
    "options": [
      "Dans le composant racine App.tsx avec useEffect",
      "Dans le store Zustand, en tant qu'action asynchrone appelée avant chaque requête protégée",
      "Dans un fichier séparé sans lien avec Zustand",
      "Uniquement dans les service workers"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "La logique de refresh token est souvent placée dans le store auth : une action `refreshToken` qui appelle l'API, puis met à jour le token. On peut aussi l'intégrer dans un interceptor Axios qui déclenche cette action du store. Cela centralise la gestion d'authentification et facilite la persistance."
  },
  {
    "questionText": "Quelle commande npm installe Zustand dans un projet React ?",
    "options": [
      "`npm install redux`",
      "`npm install @reduxjs/toolkit`",
      "`npm install zustand`",
      "`npm install react-zustand`"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La bibliothèque s'appelle officiellement `zustand` (sans préfixe `react-` car elle fonctionne aussi avec d'autres frameworks). La commande correcte est donc `npm install zustand`. `react-zustand` n'existe pas comme paquet officiel."
  },
  {
    "questionText": "Un étudiant a écrit : <code>const useAuthStore = create((set) =&gt; ({ user: null, login: async (email, pwd) =&gt; { const res = await api.login(email, pwd); set({ user: res.user, token: res.token }); } }));</code>. Comment améliorer la gestion des erreurs ?",
    "options": [
      "Ajouter un try/catch dans l'action et stocker l'erreur dans le store",
      "Ignorer les erreurs, elles seront gérées par React",
      "Utiliser un useEffect pour capturer les erreurs",
      "Remplacer Zustand par Redux Toolkit"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "Les actions asynchrones dans Zustand doivent gérer leurs propres erreurs. La bonne pratique est d'ajouter un état `error` dans le store et un bloc try/catch : `login: async (email, pwd) =&gt; { try { ... } catch(error) { set({ error: error.message }) } }`. Cela permet d'afficher un message d'erreur à l'utilisateur."
  }
]