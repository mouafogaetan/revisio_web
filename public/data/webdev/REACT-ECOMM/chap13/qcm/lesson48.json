[
  {
    "questionText": "Dans React Testing Library, quelle méthode utilise-t-on pour simuler un clic sur un bouton ?",
    "options": [
      "`fireEvent.click(button)`",
      "`simulateClick(button)`",
      "`trigger.click(button)`",
      "`button.click()`"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "`fireEvent` est la méthode fournie par React Testing Library pour déclencher des événements DOM. `fireEvent.click(element)` simule un clic utilisateur de manière fiable dans l'environnement de test."
  },
  {
    "questionText": "Quel est le rôle principal de MSW (Mock Service Worker) dans les tests d'une application React ?",
    "options": [
      "Générer des composants mockés automatiquement",
      "Intercepter les requêtes réseau au niveau du service worker et renvoyer des réponses mockées",
      "Simuler le chargement des images",
      "Tester la performance des appels API réels"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "MSW intercepte les requêtes réseau (fetch, XMLHttpRequest) au niveau du service worker et permet de renvoyer des réponses personnalisées sans avoir à modifier le code de l'application, idéal pour les tests d'intégration."
  },
  {
    "questionText": "Quelle assertion vérifie qu'un élément contenant le texte 'Panier (2)' est présent dans le document après ajout d'un produit ?",
    "options": [
      "`expect(screen.getByText('Panier (2)')).toBeVisible()`",
      "`expect(screen.getByText('Panier (2)')).toBeInTheDocument()`",
      "`expect(screen.queryByText('Panier (2)')).toBeNull()`",
      "`expect(screen.findByText('Panier (2)')).resolves.toBeDefined()`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "`toBeInTheDocument()` est un matcher de Jest-DOM qui vérifie qu'un élément existe dans le DOM. `getByText` lance une erreur si l'élément n'est pas trouvé, donc l'assertion combinée valide bien la présence."
  },
  {
    "questionText": "Dans un test de formulaire d'authentification, comment simuler la saisie d'un email dans un champ `&lt;input&gt;` avec React Testing Library ?",
    "options": [
      "`fireEvent.change(emailInput, { target: { value: 'test@example.com' } })`",
      "`userEvent.type(emailInput, 'test@example.com')`",
      "`fireEvent.input(emailInput, 'test@example.com')`",
      "Les deux A et B sont valides selon les bonnes pratiques"
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "`fireEvent.change` fonctionne, mais `userEvent.type` (de la librairie @testing-library/user-event) est recommandé car il simule plus fidèlement le comportement utilisateur (déclenche les événements keydown, keyup, change). Les deux méthodes sont acceptables, mais la meilleure pratique actuelle favorise `userEvent`."
  },
  {
    "questionText": "Pour tester le flux complet 'Authentification → Ajout au panier → Validation commande', quelle approche est la plus adaptée ?",
    "options": [
      "Trois tests unitaires séparés",
      "Un seul test d'intégration couvrant tout le scénario utilisateur",
      "Un test end-to-end avec Cypress/Playwright uniquement",
      "Un snapshot test du composant final"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Un test d'intégration unique qui suit un utilisateur réel à travers plusieurs étapes permet de valider que tous les composants et services fonctionnent ensemble. C'est plus rapide qu'un test E2E complet et plus pertinent que des tests unitaires isolés."
  },
  {
    "questionText": "Avec MSW, comment configurer un handler pour simuler une réponse `POST /api/login` avec succès ?",
    "options": [
      "`rest.post('/api/login', (req, res, ctx) =&gt; res(ctx.status(200), ctx.json({ token: 'fake-jwt' })))`",
      "`rest.get('/api/login', (req, res) =&gt; res({ token: 'fake-jwt' }))`",
      "`graphql.mutation('Login', (req, res) =&gt; res({ data: { token: 'fake' } }))`",
      "`http.post('/api/login', () =&gt; new Response(JSON.stringify({ token: 'ok' })))`"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "MSW v1 utilise `rest.post` avec `ctx` pour construire la réponse. L'option A est la syntaxe correcte pour une requête POST avec un corps JSON de succès. Les autres options sont incorrectes (GET au lieu de POST, GraphQL non adapté, ou syntaxe non standard pour MSW)."
  },
  {
    "questionText": "Quel est l'avantage d'utiliser `screen` plutôt que `getByText` directement depuis `render` ?",
    "options": [
      "`screen` permet d'accéder à tous les rôles ARIA automatiquement",
      "`screen` évite de déstructurer `render` et facilite les tests lorsque la structure change",
      "`screen` est plus rapide à l'exécution",
      "`screen` ne vérifie pas l'accessibilité"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "`screen` est un objet global qui expose toutes les requêtes (getByText, findByRole...). Son principal avantage est qu'on n'a pas besoin de déstructurer `render` et que le code est plus facile à maintenir, surtout quand on ajoute ou modifie des éléments. C'est une bonne pratique recommandée par Testing Library."
  },
  {
    "questionText": "Dans le test d'un composant `&lt;ProductCard /&gt;` avec un bouton 'Ajouter au panier', comment vérifier que la fonction `addToCart` a été appelée avec le bon produit ?",
    "options": [
      "`expect(addToCart).toHaveBeenCalledWith(expectedProduct)`",
      "`expect(addToCart.mock.calls[0][0]).toEqual(expectedProduct)`",
      "`expect(addToCart).toBeCalledWith(expect.objectContaining({ id: productId }))`",
      "Toutes ces assertions peuvent fonctionner selon la configuration du mock"
    ],
    "correctAnswer": 3,
    "difficulty": "hard",
    "explanation": "Jest offre plusieurs matchers pour les mocks : `toHaveBeenCalledWith` vérifie les arguments exacts, `mock.calls` donne un accès brut, `expect.objectContaining` permet une vérification partielle. Toutes sont valides et le choix dépend du niveau de précision souhaité."
  },
  {
    "questionText": "Quel outil est recommandé pour mocker les appels API dans un test d'intégration React moderne ?",
    "options": [
      "Jest.mock() sur le module fetch",
      "Mock Service Worker (MSW)",
      "Sinon.js",
      "nock"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "MSW est devenu la référence pour mocker les API dans les tests React car il intercepte les vraies requêtes au niveau réseau, sans avoir à modifier l'implémentation de fetch ou axios. Cela rend les tests plus réalistes et moins fragiles."
  },
  {
    "questionText": "Pourquoi utilise-t-on `waitFor` ou `findBy*` dans un test avec React ?",
    "options": [
      "Pour attendre que le composant soit monté",
      "Pour gérer les opérations asynchrones comme le chargement de données ou les animations",
      "Pour ralentir volontairement le test",
      "Pour ignorer les erreurs asynchrones"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`waitFor` et `findBy*` (ex: `findByText`) sont des utilitaires de Testing Library qui attendent qu'un élément apparaisse dans le DOM après un délai, ce qui est essentiel pour tester des composants qui chargent des données de manière asynchrone ou qui réagissent à des actions utilisateur avec des retours différés."
  }
]