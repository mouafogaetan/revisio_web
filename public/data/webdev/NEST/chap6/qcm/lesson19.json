[
  {
    "questionText": "Dans NestJS, quelle est la classe de base de toutes les exceptions gérées nativement par le framework ?",
    "options": [
      "Error",
      "HttpException",
      "BaseException",
      "NestException"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "La classe de base est `HttpException`. Toutes les exceptions HTTP standard de NestJS (comme `NotFoundException`, `BadRequestException`, etc.) héritent de cette classe."
  },
  {
    "questionText": "Quel code HTTP est automatiquement associé à l'exception `NotFoundException` lancée dans un contrôleur NestJS ?",
    "options": [
      "500 Internal Server Error",
      "403 Forbidden",
      "404 Not Found",
      "400 Bad Request"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "`NotFoundException` est une classe prédéfinie de NestJS qui étend `HttpException` avec le code HTTP par défaut **404**."
  },
  {
    "questionText": "Si vous souhaitez personnaliser le message et le code HTTP d'une exception, comment lancer correctement une `HttpException` ?",
    "options": [
      "throw new HttpException('Message personnalisé', 400);",
      "throw HttpException('Message personnalisé', 400);",
      "throw new HttpException(400, 'Message personnalisé');",
      "throw 'HttpException: Message personnalisé';"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "Le constructeur de `HttpException` accepte deux arguments obligatoires : le message (ou l'objet de réponse) et le code HTTP. La syntaxe correcte est donc `throw new HttpException('Message personnalisé', 400);`."
  },
  {
    "questionText": "Quel est le rôle principal d'un **Exception Filter** dans NestJS ?",
    "options": [
      "Intercepter les requêtes avant qu'elles n'atteignent le contrôleur",
      "Transformer la réponse JSON en XML",
      "Intercepter et formater les exceptions lancées dans l'application",
      "Gérer les connexions aux bases de données"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Le filtre d'exception (`ExceptionFilter`) est conçu pour capturer les exceptions qui se produisent à n'importe quelle étape du traitement de la requête et pour renvoyer une réponse cohérente et bien formatée au client."
  },
  {
    "questionText": "Vous devez créer un filtre d'exception qui s'applique à TOUS les contrôleurs et routes de l'application. Où devez-vous l'enregistrer ?",
    "options": [
      "Dans chaque module, via `providers: [MyFilter]`",
      "Dans le fichier `main.ts`, via `app.useGlobalFilters(new MyFilter())`",
      "Directement sur chaque contrôleur avec `@UseFilters(MyFilter)`",
      "Dans le dossier `exceptions`, NestJS le détecte automatiquement"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Pour une portée globale, on utilise l'instance de l'application (`app.useGlobalFilters()`). L'enregistrement dans `main.ts` permet au filtre d'intercepter toutes les exceptions non gérées de tous les contextes."
  },
  {
    "questionText": "Quel décorateur utilisez-vous pour appliquer un filtre d'exception personnalisé UNIQUEMENT à une route spécifique d'un contrôleur ?",
    "options": [
      "@ExceptionFilter()",
      "@Catch()",
      "@Filter()",
      "@UseFilters()"
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "Le décorateur `@UseFilters()` peut être appliqué au niveau de la méthode d'un contrôleur, du contrôleur entier ou globalement. Il accepte une ou plusieurs classes de filtres personnalisés."
  },
  {
    "questionText": "Voici un extrait de code :\n```typescript\n@Catch(NotFoundException)\nexport class NotFoundFilter implements ExceptionFilter {\n  catch(exception: NotFoundException, host: ArgumentsHost) {\n    // ...\n  }\n}\n```\nQue signifie `@Catch(NotFoundException)` ?",
    "options": [
      "Le filtre ne s'exécutera que si l'exception n'est PAS trouvée",
      "Le filtre capture uniquement les exceptions de type `NotFoundException`",
      "Le filtre capture TOUTES les exceptions, sans exception",
      "Le filtre ignore les exceptions de type `NotFoundException`"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Le décorateur `@Catch()` permet de spécifier quelles exceptions le filtre doit intercepter. Ici, le filtre se déclenche uniquement pour les exceptions `NotFoundException` et ses sous-classes."
  },
  {
    "questionText": "Dans un filtre d'exception personnalisé, l'objet `ArgumentsHost` permet d'accéder à la requête et à la réponse. Quelle méthode utilisez-vous pour obtenir l'objet `Response` Express/Fastify ?",
    "options": [
      "host.getResponse()",
      "host.switchToHttp().getResponse()",
      "host.getResponseObject()",
      "host.getArgs()[1]"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Pour une application HTTP, il faut d'abord basculer vers le contexte HTTP avec `host.switchToHttp()`, puis appeler `getResponse()`. Cette approche rend le filtre compatible avec plusieurs plateformes (HTTP, WebSockets, RPC)."
  },
  {
    "questionText": "Vous lancez `throw new BadRequestException('Donnée invalide');`. Sans filtre personnalisé, quel sera le format approximatif de la réponse JSON ?",
    "options": [
      "{ \"status\": 400, \"message\": \"Bad Request\" }",
      "{ \"statusCode\": 400, \"message\": \"Bad Request\", \"error\": \"Donnée invalide\" }",
      "{ \"error\": \"BadRequestException\", \"text\": \"Donnée invalide\" }",
      "{ \"code\": 400, \"body\": \"Donnée invalide\" }"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Par défaut, NestJS construit une réponse avec `statusCode`, `message` (le message par défaut de l'exception) et `error` (le message personnalisé passé en argument). L'objet complet peut varier selon le constructeur utilisé."
  },
  {
    "questionText": "Vous devez lancer une exception avec un message d'erreur ET des propriétés supplémentaires (ex: `{ message: 'Erreur', code_metier: 42 }`). Comment faire ?",
    "options": [
      "throw new HttpException('{ message: \"Erreur\", code_metier: 42 }', 400);",
      "throw new HttpException({ message: 'Erreur', code_metier: 42 }, 400);",
      "throw new BadRequestException(400, { code_metier: 42 });",
      "Impossible, NestJS n'autorise que le message simple"
    ],
    "correctAnswer": 1,
    "difficulty": "very hard",
    "explanation": "Le premier argument de `HttpException` (et des classes dérivées) peut être un objet littéral. Cet objet sera utilisé comme corps de la réponse HTTP, ce qui permet de structurer finement les erreurs métier."
  }
]