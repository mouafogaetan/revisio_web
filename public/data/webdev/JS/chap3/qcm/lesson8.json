[
  {
    "questionText": "En JavaScript, quel mot-clé permet d'exécuter un bloc de code uniquement si une condition est vraie ?",
    "options": [
      "`for`",
      "`if`",
      "`while`",
      "`switch`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Le mot-clé `if` est la structure conditionnelle de base en JavaScript. Le code entre les accolades `{}` n'est exécuté que si l'expression entre parenthèses est évaluée à `true`."
  },
  {
    "questionText": "Quelle est la sortie du code suivant ?\n\n```javascript\nlet age = 17;\nif (age &gt;= 18) {\n    console.log(\"Majeur\");\n} else {\n    console.log(\"Mineur\");\n}\n```",
    "options": [
      "`Majeur`",
      "`Mineur`",
      "`undefined`",
      "Une erreur"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "La condition `age &gt;= 18` est fausse car 17 n'est pas supérieur ou égal à 18. Le programme exécute donc le bloc `else`, qui affiche `Mineur` dans la console."
  },
  {
    "questionText": "Comment écrire correctement une chaîne de conditions `if...else if...else` pour tester plusieurs cas exclusifs ?",
    "options": [
      "`if (x &gt; 0) {...} else if (x === 0) {...} else {...}`",
      "`if (x &gt; 0) {...} elseif (x === 0) {...} else {...}`",
      "`if (x &gt; 0) {...} else if (x === 0) {...} elif {...}`",
      "`if (x &gt; 0) {...} else (x === 0) {...} else {...}`"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "La syntaxe correcte est `if (condition1) {...} else if (condition2) {...} else {...}`. Les mots-clés `else if` sont séparés par un espace. Les autres propositions utilisent des mots-clés inexistants (`elseif`, `elif`) ou une syntaxe incorrecte."
  },
  {
    "questionText": "Quel est l'avantage principal de l'opérateur ternaire (`condition ? valeurSiVrai : valeurSiFaux`) par rapport à une structure `if...else` ?",
    "options": [
      "Il est plus rapide à l'exécution.",
      "Il permet de créer des boucles.",
      "Il permet d'écrire une condition simple de manière plus concise, souvent sur une ligne.",
      "Il peut gérer plus de deux cas sans `else if`."
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "L'opérateur ternaire est un sucre syntaxique qui rend le code plus court et plus lisible pour des affectations conditionnelles simples. Par exemple, `let statut = age &gt;= 18 ? 'majeur' : 'mineur'` est plus concis que son équivalent `if...else`."
  },
  {
    "questionText": "Quelle sera la valeur de la variable `type` après l'exécution de ce code ?\n\n```javascript\nlet nombre = 0;\nlet type = nombre &gt; 0 ? \"positif\" : nombre &lt; 0 ? \"negatif\" : \"nul\";\n```",
    "options": [
      "`\"positif\"`",
      "`\"negatif\"`",
      "`\"nul\"`",
      "`undefined`"
    ],
    "correctAnswer": 2,
    "difficulty": "hard",
    "explanation": "Il s'agit d'un opérateur ternaire chaîné (imbriqué). Il se lit de droite à gauche ou en analysant les conditions : `nombre &gt; 0` est faux (`0 &gt; 0` = faux), donc on évalue la partie après les `:` : `nombre &lt; 0 ? \"negatif\" : \"nul\"`. `0 &lt; 0` est faux, donc on prend la valeur après le `:` final, qui est `\"nul\"`."
  },
  {
    "questionText": "Quelle est l'équivalence en utilisant l'opérateur ternaire de ce bloc `if...else` ?\n\n```javascript\nlet message;\nif (estConnecte) {\n    message = \"Bienvenue !\";\n} else {\n    message = \"Veuillez vous connecter.\";\n}\n```",
    "options": [
      "`let message = estConnecte ? \"Bienvenue !\" : \"Veuillez vous connecter.\";`",
      "`estConnecte ? message = \"Bienvenue !\" : message = \"Veuillez vous connecter.\";`",
      "`let message = if (estConnecte) \"Bienvenue !\" else \"Veuillez vous connecter.\";`",
      "`let message = estConnecte && \"Bienvenue !\" || \"Veuillez vous connecter.\";`"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "L'opérateur ternaire est une expression qui retourne une valeur. La syntaxe correcte pour l'affecter à une variable est `let message = condition ? valeurSiVrai : valeurSiFaux`. L'option 0 est la seule qui respecte cette syntaxe et cette logique."
  },
  {
    "questionText": "Que se passe-t-il si on oublie les accolades `{}` après un `if` ou un `else` ?",
    "options": [
      "Le code ne s'exécute pas du tout.",
      "Une erreur de syntaxe est levée.",
      "Une erreur d'exécution se produit.",
      "Seule l'instruction immédiatement après le `if` ou le `else` est conditionnelle."
    ],
    "correctAnswer": 3,
    "difficulty": "hard",
    "explanation": "En JavaScript, les accolades sont optionnelles si le bloc ne contient qu'une seule instruction. Dans ce cas, seule la première instruction qui suit le `if` (ou le `else`) est soumise à la condition. C'est une source fréquente de bugs."
  },
  {
    "questionText": "Quelle valeur sera affichée ?\n\n```javascript\nlet x = 10;\nif (x = 5) {\n    console.log(\"Vrai\");\n} else {\n    console.log(\"Faux\");\n}\n```",
    "options": [
      "`\"Faux\"`",
      "Une erreur de syntaxe",
      "`\"Vrai\"`",
      "Rien, la console est vide"
    ],
    "correctAnswer": 2,
    "difficulty": "very hard",
    "explanation": "C'est un piège classique. Dans la condition, on utilise `=` (affectation) et non `==` ou `===` (comparaison). L'expression `x = 5` affecte la valeur 5 à `x`, puis cette expression elle-même est évaluée. La valeur d'une affectation est la valeur affectée (5). 5 est une valeur \"truthy\" (convertie en `true` dans un contexte booléen). Le code affiche donc `\"Vrai\"`."
  }
]