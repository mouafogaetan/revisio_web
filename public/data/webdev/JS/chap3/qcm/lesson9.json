[
  {
    "questionText": "Quelle est l'utilité principale de la structure `switch` en JavaScript ?",
    "options": [
      "Exécuter une boucle un nombre défini de fois",
      "Comparer une même expression ou variable à plusieurs valeurs possibles",
      "Vérifier une condition complexe avec des opérateurs logiques (&&, ||)",
      "Déclarer une variable locale dans un bloc"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "La structure `switch` est spécialement conçue pour comparer une expression ou une variable contre plusieurs cas (`case`). Elle est plus lisible qu'une succession de `else if` lorsqu'on teste une seule valeur contre de nombreuses possibilités."
  },
  {
    "questionText": "Que se passe-t-il si on omet l'instruction `break` dans un `case` d'un `switch` ?",
    "options": [
      "Le code s'arrête automatiquement à la fin du case",
      "Une erreur est levée par le navigateur",
      "L'exécution 'tombe en cascade' (fall-through) et continue dans le case suivant",
      "La condition du case est réévaluée une nouvelle fois"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "Sans `break`, JavaScript continue d'exécuter le code des cases suivants, y compris le `default`, jusqu'à rencontrer un `break` ou la fin du `switch`. C'est ce qu'on appelle 'fall-through'. Parfois utilisé volontairement, mais souvent source d'erreurs."
  },
  {
    "questionText": "Dans un `switch`, quel mot-clé permet d'exécuter un bloc de code si aucun `case` ne correspond ?",
    "options": [
      "else",
      "otherwise",
      "final",
      "default"
    ],
    "correctAnswer": 3,
    "difficulty": "easy",
    "explanation": "Le mot-clé `default` joue le rôle du `else` dans un `switch`. Il est optionnel mais recommandé pour gérer les valeurs inattendues. Il se place généralement en dernier, mais ce n'est pas obligatoire."
  },
  {
    "questionText": "Soit le code suivant. Quelle est la valeur affichée dans la console ?\n\n```javascript\nlet fruit = \"ORANGE\";\nswitch (fruit) {\n  case \"pomme\":\n    console.log(\"50 FCFA\");\n    break;\n  case \"orange\":\n    console.log(\"30 FCFA\");\n    break;\n  default:\n    console.log(\"Fruit non trouvé\");\n}\n```",
    "options": [
      "50 FCFA",
      "30 FCFA",
      "Fruit non trouvé",
      "undefined"
    ],
    "correctAnswer": 2,
    "difficulty": "hard",
    "explanation": "La comparaison dans un `switch` est stricte (===). La variable `fruit` contient \"ORANGE\" (en majuscules), tandis que les `case` comparent avec des chaînes en minuscules (\"pomme\", \"orange\"). Comme aucune correspondance stricte n'est trouvée, c'est le bloc `default` qui s'exécute."
  },
  {
    "questionText": "Quel mot-clé est strictement équivalent à l'utilisation de `break` dans chaque `case` d'un `switch` pour en sortir immédiatement ?",
    "options": [
      "continue",
      "return",
      "exit",
      "Il n'y a pas d'équivalent automatique, break est nécessaire"
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "À la différence d'autres langages, JavaScript ne sort pas automatiquement d'un `case`. Sans `break` (ou `return` si le switch est dans une fonction), l'exécution continue. Il n'existe pas de mot-clé pour remplacer `break` dans ce contexte précis."
  },
  {
    "questionText": "Comment regrouper plusieurs valeurs pour qu'elles exécutent le même bloc de code dans un `switch` ?",
    "options": [
      "case 1, 2, 3:",
      "case 1 || 2 || 3:",
      "case 1: case 2: case 3:",
      "case [1,2,3]:"
    ],
    "correctAnswer": 2,
    "difficulty": "hard",
    "explanation": "Pour que plusieurs valeurs partagent le même code, on écrit les `case` successivement sans `break` entre eux. Exemple :\n```javascript\nswitch(valeur) {\n  case 1:\n  case 2:\n  case 3:\n    console.log(\"1, 2 ou 3\");\n    break;\n}\n```"
  }
]