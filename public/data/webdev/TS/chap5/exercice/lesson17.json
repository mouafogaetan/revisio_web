[
  {
    "intitule": "Distinguer les modificateurs d'accès public, private et protected",
    "type": "ressource",
    "niveau": "easy",
    "enonce": {
      "texte": "<p>TypeScript propose trois modificateurs d'accès pour les propriétés et méthodes d'une classe.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Expliquer la différence entre `public`, `private` et `protected` dans une classe TypeScript.",
        "niveau": "easy",
        "reponse": "- **public** (par défaut) : accessible depuis n'importe où (à l'intérieur de la classe, depuis les instances, et depuis les classes héritées). - **private** : accessible uniquement à l'intérieur de la classe où elle est déclarée. Ni les instances ni les classes héritées ne peuvent y accéder. - **protected** : accessible à l'intérieur de la classe et dans les classes héritées, mais pas depuis les instances.",
        "sousQuestions": []
      },
      {
        "numero": 2,
        "texte": "Une propriété `private` peut-elle être lue ou modifiée en dehors de sa classe ? Justifier.",
        "niveau": "easy",
        "reponse": "Non, une propriété `private` n'est accessible qu'à l'intérieur de la classe où elle est déclarée. Toute tentative d'accès depuis l'extérieur (instance ou classe héritée) génère une erreur de compilation. Cela permet d'encapsuler les détails internes d'une classe.",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Appliquer les modificateurs d'accès dans une classe BankAccount",
    "type": "competence",
    "niveau": "medium",
    "enonce": {
      "texte": "<p>Créer une classe `BankAccount` avec : un solde `balance` (privé, initialisé à 0), un propriétaire `owner` (public, en lecture seule), une méthode `deposit(amount: number)` (publique) qui augmente le solde si le montant est positif, et une méthode `getBalance()` (publique) pour consulter le solde.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Implémenter la classe `BankAccount` avec les modificateurs appropriés.",
        "niveau": "medium",
        "reponse": "```typescript\nclass BankAccount {\n  private balance: number = 0;\n  public readonly owner: string;\n\n  constructor(owner: string) {\n    this.owner = owner;\n  }\n\n  public deposit(amount: number): void {\n    if (amount > 0) {\n      this.balance += amount;\n    }\n  }\n\n  public getBalance(): number {\n    return this.balance;\n  }\n}\n\nconst account = new BankAccount(\"Alice\");\naccount.deposit(100);\nconsole.log(account.getBalance()); // 100\n// account.balance = 500; // Erreur : propriété private\n```",
        "sousQuestions": []
      },
      {
        "numero": 2,
        "texte": "Pourquoi `balance` est-elle déclarée `private` plutôt que `protected` ?",
        "niveau": "medium",
        "reponse": "`balance` est déclarée `private` car le solde ne doit être modifiable que par les méthodes internes de `BankAccount` (`deposit`, `withdraw`). Aucune classe héritée ne devrait pouvoir modifier directement le solde, car cela pourrait violer l'intégrité des données (ex: une classe fille pourrait diminuer le solde sans contrôle). `private` garantit un encapsulage strict.",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Maîtriser les accesseurs get et set",
    "type": "competence",
    "niveau": "medium",
    "enonce": {
      "texte": "<p>Ajouter un accesseur `get` et `set` pour la propriété `balance` dans la classe `BankAccount`. Le `set` doit interdire les valeurs négatives et ne doit pas permettre de descendre en dessous de 0.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Remplacer la méthode `getBalance()` par un accesseur `get balance()` et ajouter un accesseur `set balance()` avec la validation appropriée.",
        "niveau": "medium",
        "reponse": "```typescript\nclass BankAccount {\n  private _balance: number = 0;\n  public readonly owner: string;\n\n  constructor(owner: string) {\n    this.owner = owner;\n  }\n\n  get balance(): number {\n    return this._balance;\n  }\n\n  set balance(amount: number) {\n    if (amount >= 0) {\n      this._balance = amount;\n    } else {\n      console.error(\"Le solde ne peut pas être négatif\");\n    }\n  }\n\n  public deposit(amount: number): void {\n    if (amount > 0) {\n      this.balance = this._balance + amount;\n    }\n  }\n}\n\nconst account = new BankAccount(\"Alice\");\naccount.balance = 100; // Utilise le setter\nconsole.log(account.balance); // 100 (utilise le getter)\naccount.balance = -50; // Erreur: refusé\n```",
        "sousQuestions": []
      },
      {
        "numero": 2,
        "texte": "Quel est l'avantage d'utiliser `get`/`set` plutôt qu'une propriété publique ?",
        "niveau": "medium",
        "reponse": "Les accesseurs permettent d'ajouter une logique de validation ou de transformation lors de l'accès ou de l'assignation d'une propriété, tout en conservant une syntaxe d'accès simple (comme une propriété). Si on avait une propriété publique `balance`, on ne pourrait pas contrôler les assignations (valeur négative, montant trop élevé). Avec `set`, on peut valider. Avec `get`, on peut calculer une valeur à la volée ou restreindre la visibilité.",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Comprendre la différence entre private et # (private field)",
    "type": "ressource",
    "niveau": "hard",
    "enonce": {
      "texte": "<p>TypeScript supporte deux manières de déclarer des membres privés : le modificateur `private` (TypeScript) et le champ privé JavaScript `#` (ECMAScript).</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Expliquer la différence fondamentale entre `private` et `#field` en termes de confidentialité à l'exécution.",
        "niveau": "hard",
        "reponse": "`private` est une vérification **uniquement à la compilation** par TypeScript. Dans le code JavaScript généré, la propriété `private` est simplement une propriété normale (accessible à l'exécution). `#field` (champ privé JavaScript) est une véritable confidentialité au niveau du langage : le champ est inaccessible à l'extérieur même à l'exécution (erreur si on tente d'y accéder). Avec `#`, on ne peut pas utiliser `account['#balance']` ni `Object.keys()`. Le champ `#` est plus sécurisé mais moins flexible (pas de `protected` par exemple).",
        "sousQuestions": []
      },
      {
        "numero": 2,
        "texte": "Dans quel cas préférer `#` plutôt que `private` ?",
        "niveau": "hard",
        "reponse": "On préfère `#` quand on veut une confidentialité **garantie même à l'exécution**, par exemple pour une bibliothèque distribuée où des utilisateurs pourraient accéder au code JS. Avec `#`, même en inspectant l'objet à l'exécution, le champ n'est pas accessible. `private` suffit pour la plupart des applications internes (le compilateur protège pendant le développement). `#` est également utile pour éviter les collisions de noms avec des propriétés héritées.",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Utiliser readonly pour les propriétés immutables",
    "type": "competence",
    "niveau": "easy",
    "enonce": {
      "texte": "<p>Créer une classe `Point` avec des coordonnées `x` et `y` en lecture seule (initialisées dans le constructeur) et une méthode `distanceTo(other: Point): number`.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Implémenter la classe `Point` avec la syntaxe la plus concise possible (raccourci constructeur).",
        "niveau": "easy",
        "reponse": "```typescript\nclass Point {\n  constructor(\n    public readonly x: number,\n    public readonly y: number\n  ) {}\n\n  distanceTo(other: Point): number {\n    const dx = this.x - other.x;\n    const dy = this.y - other.y;\n    return Math.sqrt(dx ** 2 + dy ** 2);\n  }\n}\n\nconst p1 = new Point(0, 0);\nconst p2 = new Point(3, 4);\nconsole.log(p1.distanceTo(p2)); // 5\n// p1.x = 10; // Erreur: ne peut pas modifier une propriété readonly\n```",
        "sousQuestions": []
      },
      {
        "numero": 2,
        "texte": "Que se passe-t-il si on essaie de modifier `x` après l'initialisation ?",
        "niveau": "easy",
        "reponse": "TypeScript génère une erreur de compilation : `Cannot assign to 'x' because it is a read-only property`. `readonly` garantit que la propriété ne peut être modifiée après la construction, ce qui est idéal pour les objets immutables comme un point.",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Analyser un code existant avec modificateurs d'accès",
    "type": "ressource",
    "niveau": "medium",
    "enonce": {
      "texte": "<p>Voici une classe TypeScript :</p><pre><code>class Employee {\n  public name: string;\n  private salary: number;\n  protected department: string;\n\n  constructor(name: string, salary: number, department: string) {\n    this.name = name;\n    this.salary = salary;\n    this.department = department;\n  }\n}\n\nclass Manager extends Employee {\n  public getInfo(): string {\n    return `${this.name} - ${this.department}`;\n  }\n}\n\nconst emp = new Employee(\"Alice\", 50000, \"IT\");\nconsole.log(emp.name);        // Ligne A\nconsole.log(emp.salary);      // Ligne B\nconsole.log(emp.department);  // Ligne C\nconst mgr = new Manager(\"Bob\", 80000, \"Sales\");\nconsole.log(mgr.getInfo());   // Ligne D\nmgr.department = \"Marketing\"; // Ligne E\n```",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Indiquer pour chaque ligne (A à E) si elle est valide ou génère une erreur. Justifier.",
        "niveau": "medium",
        "reponse": "- **Ligne A** : valide. `name` est public, accessible depuis l'instance. - **Ligne B** : erreur. `salary` est private, non accessible en dehors de la classe `Employee`. - **Ligne C** : erreur. `department` est protected, accessible uniquement dans `Employee` et ses classes filles, pas depuis une instance. - **Ligne D** : valide. `getInfo()` est public et accède à `name` (public) et `department` (protected mais accessible depuis `Manager` car c'est une classe fille). - **Ligne E** : valide. `department` est protected, donc accessible et modifiable depuis `Manager` (classe fille).",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Créer une classe avec propriétés privées et accesseurs",
    "type": "competence",
    "niveau": "hard",
    "enonce": {
      "texte": "<p>Créer une classe `Thermostat` avec une propriété privée `_temperature` en Celsius. Ajouter un accesseur `get temperature()` qui retourne la température en Celsius, et un accesseur `set temperature(value: number)` qui accepte une valeur en Celsius mais la stocke après conversion en Fahrenheit (ou l'inverse, au choix).</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Implémenter la classe avec la conversion choisie (expliquer la logique).",
        "niveau": "hard",
        "reponse": "```typescript\nclass Thermostat {\n  private _celsius: number = 0;\n\n  get temperature(): number {\n    // Retourne la température en Celsius\n    return this._celsius;\n  }\n\n  set temperature(celsius: number) {\n    if (celsius < -273.15) {\n      throw new Error(\"Température inférieure au zéro absolu\");\n    }\n    this._celsius = celsius;\n  }\n\n  // Optionnel : getter pour Fahrenheit\n  get fahrenheit(): number {\n    return this._celsius * 9/5 + 32;\n  }\n}\n\nconst thermo = new Thermostat();\n thermo.temperature = 25;\nconsole.log(thermo.temperature); // 25\nconsole.log(thermo.fahrenheit);  // 77\n```\nLe setter permet de valider la plage de température. L'utilisateur n'a pas besoin de connaître l'unité interne (Celsius), il manipule la température via `temperature`.",
        "sousQuestions": []
      },
      {
        "numero": 2,
        "texte": "Pourquoi utilise-t-on une propriété privée `_celsius` plutôt que d'avoir une propriété publique `celsius` et un accesseur ?",
        "niveau": "hard",
        "reponse": "La propriété privée `_celsius` est le stockage interne réel. L'accesseur `temperature` agit comme une interface publique. Cela permet de : 1) Valider les assignations (impossible avec une propriété publique). 2) Changer l'implémentation interne (ex: stocker en Fahrenheit) sans modifier l'API publique. 3) Ajouter des effets de bord (logs, notifications) lors de l'accès ou de l'assignation.",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Comparer l'héritage et l'encapsulation avec protected",
    "type": "competence",
    "niveau": "hard",
    "enonce": {
      "texte": "<p>Créer une classe de base `Vehicle` avec une propriété `protected speed` et une méthode `accelerate()`. Créer deux classes filles `Car` et `Bicycle` qui héritent de `Vehicle` et modifient le comportement d'accélération.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Implémenter les trois classes.",
        "niveau": "hard",
        "reponse": "```typescript\nclass Vehicle {\n  protected speed: number = 0;\n\n  accelerate(increment: number): void {\n    this.speed += increment;\n  }\n\n  getSpeed(): number {\n    return this.speed;\n  }\n}\n\nclass Car extends Vehicle {\n  accelerate(increment: number): void {\n    // Une voiture accélère plus vite\n    this.speed += increment * 2;\n  }\n}\n\nclass Bicycle extends Vehicle {\n  accelerate(increment: number): void {\n    // Un vélo accélère plus lentement\n    this.speed += increment * 0.5;\n  }\n}\n\nconst car = new Car();\ncar.accelerate(10);\nconsole.log(car.getSpeed()); // 20\n\nconst bike = new Bicycle();\nbike.accelerate(10);\nconsole.log(bike.getSpeed()); // 5\n```\nLa propriété `speed` est `protected`, donc accessible depuis les classes filles, mais pas depuis l'extérieur. Cela permet l'héritage tout en maintenant l'encapsulation.",
        "sousQuestions": []
      },
      {
        "numero": 2,
        "texte": "Que se passe-t-on si `speed` était déclarée `private` à la place de `protected` ?",
        "niveau": "hard",
        "reponse": "Si `speed` était `private`, les classes filles `Car` et `Bicycle` ne pourraient pas accéder directement à `this.speed`. Elles devraient utiliser un getter/setter public ou appeler la méthode `accelerate()` de la classe parente via `super.accelerate(increment)`. Elles ne pourraient pas redéfinir le facteur d'accélération aussi simplement. `protected` est donc plus approprié pour les propriétés destinées à être utilisées par les classes dérivées.",
        "sousQuestions": []
      }
    ]
  }
]