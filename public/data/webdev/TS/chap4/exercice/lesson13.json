[
  {
    "questionText": "Quelle est la syntaxe correcte pour définir une interface `User` avec une propriété `name` de type string et une propriété optionnelle `age` de type number ?",
    "options": [
      "interface User { name: string; age?: number; }",
      "type User { name: string; age: number | undefined; }",
      "interface User { name: string; age: number?; }",
      "const User = { name: string; age?: number; }"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "La syntaxe correcte pour une propriété optionnelle est d'ajouter un point d'interrogation après le nom de la propriété : `age?: number`. Cela signifie que la propriété peut être absente de l'objet (ou valoir `undefined`). L'option 2 utilise `type` (qui est un alias de type, pas une interface) et une union avec `undefined` (différent d'optionnel). Les options 3 et 4 utilisent des syntaxes invalides."
  },
  {
    "questionText": "Comment déclare-t-on une propriété en lecture seule (immutable) dans une interface TypeScript ?",
    "options": [
      "const property: string;",
      "readonly property: string;",
      "final property: string;",
      "immutable property: string;"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Le mot-clé `readonly` permet de définir une propriété qui ne peut être modifiée après l'initialisation de l'objet. Tentative d'assignation ultérieure génère une erreur de compilation : `Cannot assign to 'property' because it is a read-only property`. C'est l'équivalent de `Object.freeze()` au niveau du type."
  },
  {
    "questionText": "Quelle est la différence principale entre `interface` et `type` en TypeScript ?",
    "options": [
      "`interface` est pour les objets, `type` pour les primitifs uniquement",
      "`interface` peut être étendue et supporte la déclaration merging, contrairement à `type`",
      "`type` est plus rapide à l'exécution que `interface`",
      "Il n'y a aucune différence, ils sont interchangeables"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Les interfaces supportent la **déclaration merging** : si on déclare deux interfaces avec le même nom, elles sont automatiquement fusionnées. Les `type` ne supportent pas cette fonctionnalité. De plus, les interfaces sont préférables pour définir la forme d'objets (POO, implémentation par des classes), tandis que les `type` sont plus flexibles (unions, tuples, types primitifs, mapped types). Les deux peuvent être étendus (`extends` pour interface, `&` pour type)."
  },
  {
    "questionText": "Que se passe-t-il si on déclare deux interfaces avec le même nom dans le même fichier ?",
    "options": [
      "Une erreur de compilation (nom dupliqué)",
      "Les deux interfaces sont fusionnées (declaration merging)",
      "La seconde interface écrase la première",
      "Le code ne compile que si les deux interfaces sont identiques"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "TypeScript fusionne automatiquement les interfaces portant le même nom. C'est ce qu'on appelle la **declaration merging**. Exemple : `interface User { name: string }` et `interface User { age: number }` donne une interface `User` avec `name` et `age`. Cela permet d'étendre des interfaces existantes (notamment pour les bibliothèques). Les `type` ne supportent pas cette fonctionnalité."
  },
  {
    "questionText": "Comment déclarer une interface `Person` qui hérite (étend) de `Animal` en TypeScript ?",
    "options": [
      "interface Person implements Animal { ... }",
      "interface Person extends Animal { ... }",
      "interface Person : Animal { ... }",
      "type Person = Animal & { ... }"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Une interface peut hériter d'une autre interface avec le mot-clé `extends`. Exemple : `interface Animal { name: string }` et `interface Person extends Animal { age: number }`. `Person` aura alors `name` (hérité) et `age`. On peut aussi hériter de plusieurs interfaces : `interface C extends A, B`. L'option 2 (`type`) est une intersection, pas une interface."
  },
  {
    "questionText": "Quelle est la particularité de la propriété `age` dans cette interface ? `interface User { name: string; age?: number; }`",
    "options": [
      "Elle est en lecture seule",
      "Elle est optionnelle (peut être absente de l'objet)",
      "Elle est privée",
      "Elle est de type `number | null`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Le point d'interrogation `?` rend la propriété **optionnelle**. Un objet de type `User` peut exister sans la propriété `age`. Si la propriété est présente, elle doit être de type `number`. C'est différent de `age: number | undefined` car une propriété optionnelle peut être complètement absente (non définie dans l'objet littéral), alors que `undefined` signifie que la clé existe mais a une valeur `undefined`."
  },
  {
    "questionText": "Quel est l'avantage d'utiliser `readonly` sur une propriété d'interface plutôt qu'un simple `const` dans l'implémentation ?",
    "options": [
      "`readonly` est vérifié à l'exécution, `const` à la compilation",
      "`readonly` garantit que la propriété ne sera pas modifiée à travers toutes les références, pas seulement la variable initiale",
      "`const` est plus restrictif que `readonly`",
      "Il n'y a pas de différence; ce sont des synonymes"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "`readonly` dans une interface s'applique à la propriété de l'objet : quelle que soit la référence (objet original, paramètre de fonction, alias), on ne pourra pas assigner une nouvelle valeur à cette propriété. `const` sur une variable empêche la réassignation de cette variable, mais si cette variable contient un objet, ses propriétés peuvent être modifiées. `readonly` est plus fort pour l'immutabilité des propriétés d'objet."
  },
  {
    "questionText": "Que signifie l'erreur `Property 'email' does not exist on type 'User'` ?",
    "options": [
      "La variable `User` n'existe pas",
      "L'objet passé comme `User` ne contient pas la propriété `email` attendue par l'interface",
      "Le fichier TypeScript n'a pas été compilé",
      "Il faut installer un package @types/user"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Cette erreur signifie qu'on essaie d'accéder à une propriété `email` sur un objet typé `User`, mais cette propriété n'est pas déclarée dans l'interface `User`. TypeScript vérifie que les accès aux propriétés respectent le contrat défini par l'interface. C'est le mécanisme fondamental du typage structurel (duck typing) de TypeScript."
  },
  {
    "questionText": "Quelle est la différence entre `interface` et `type` pour définir un tuple ?",
    "options": [
      "Seul `type` permet de définir des tuples : `type Point = [number, number]`",
      "Les deux permettent de définir des tuples de la même façon",
      "Seule l'interface permet de définir des tuples : `interface Point { 0: number; 1: number }`",
      "Les tuples ne peuvent pas être typés en TypeScript"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "Les **interfaces ne peuvent pas définir des tuples** de manière concise. On peut utiliser une interface avec des index signatures numériques, mais ce n'est pas pratique. La syntaxe recommandée pour les tuples est `type Point = [number, number]`. Les interfaces sont conçues pour décrire des objets avec des propriétés nommées, tandis que les `type` sont plus flexibles (unions, tuples, types primitifs, mapped types)."
  },
  {
    "questionText": "Comment peut-on rendre toutes les propriétés d'une interface optionnelles ?",
    "options": [
      "`interface PartialUser extends User { ? }`",
      "En utilisant le type utilitaire `Partial<User>`",
      "En ajoutant `?` devant chaque propriété manuellement",
      "`interface UserOptional = { [K in keyof User]?: User[K] }`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "TypeScript fournit un type utilitaire générique `Partial<T>` qui rend toutes les propriétés de `T` optionnelles. Exemple : `type PartialUser = Partial<User>`. Cela évite d'avoir à recréer une interface manuellement. Les options 0 et 2 sont des syntaxes invalides, et l'option 3 est un mapped type maison qui fonctionnerait mais moins pratique que `Partial`."
  },
  {
    "questionText": "Quelle est la sortie de la compilation suivante ? (strictNullChecks activé) `interface Config { url: string; timeout?: number; } const c: Config = { url: \"api.com\" }; console.log(c.timeout.toFixed(2));`",
    "options": [
      "`0.00`",
      "`undefined`",
      "Erreur d'exécution (Cannot read property 'toFixed' of undefined)",
      "Erreur de compilation (Object is possibly 'undefined')"
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "Avec `strictNullChecks: true`, TypeScript détecte que `timeout` est optionnel, donc potentiellement `undefined`. L'appel à `toFixed()` génère une erreur de compilation : `Object is possibly 'undefined'`. C'est la puissance du typage : on est obligé de vérifier l'existence avant d'utiliser la propriété (`if (c.timeout) c.timeout.toFixed(2)`) ou d'utiliser l'optional chaining (`c.timeout?.toFixed(2)`)."
  },
  {
    "questionText": "Que fait l'opérateur `keyof` appliqué à une interface ? `interface User { name: string; age: number; } type UserKeys = keyof User;`",
    "options": [
      "`type UserKeys = string`",
      "`type UserKeys = \"name\" | \"age\"`",
      "`type UserKeys = \"name\" & \"age\"`",
      "`type UserKeys = \"name\"`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "L'opérateur `keyof` retourne un **type union** des clés (noms de propriétés) d'un type ou d'une interface. Pour `User`, `keyof User` donne `\"name\" | \"age\"`. C'est très utile pour créer des types contraints, comme `function getProperty<T, K extends keyof T>(obj: T, key: K)`, ou pour les mapped types."
  },
  {
    "questionText": "Quelle est la différence entre une propriété `age?: number` et `age: number | undefined` dans une interface ?",
    "options": [
      "Aucune différence, les deux sont strictement équivalents",
      "`age?: number` permet d'omettre complètement la propriété, `age: number | undefined` exige que la propriété existe mais puisse être `undefined`",
      "`age: number | undefined` est plus lent à l'exécution",
      "`age?: number` n'est pas autorisé dans les interfaces"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Avec `age?: number`, on peut écrire `{ name: \"Alice\" }` (propriété `age` complètement absente). Avec `age: number | undefined`, l'objet doit avoir la propriété `age`, même si sa valeur est `undefined` : `{ name: \"Alice\", age: undefined }`. La propriété optionnelle est donc plus flexible car elle ne force pas la présence de la clé dans l'objet. Cela a des implications pour l'itération (`Object.keys`) et le spread operator."
  },
  {
    "questionText": "Quelle est l'utilité de l'index signature dans une interface ? `interface Dict { [key: string]: number; }`",
    "options": [
      "Déclarer un tableau de nombres",
      "Permettre à l'objet d'avoir des propriétés avec des noms dynamiques, toutes devant être de type number",
      "Définir une propriété appelée `key` de type string",
      "Créer un dictionnaire en lecture seule"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "L'index signature `[key: string]: number` indique que l'objet peut avoir n'importe quel nombre de propriétés avec des noms de type `string`, et toutes ces propriétés doivent être de type `number`. Exemple : `const dict: Dict = { a: 1, b: 2, c: 3 }`. Cela permet de typer des dictionnaires ou des objets aux propriétés dynamiques. On peut aussi avoir `[key: number]: string` pour des tableaux indexés par des nombres."
  },
  {
    "questionText": "Dans une classe TypeScript, que permet le mot-clé `implements` avec une interface ?",
    "options": [
      "La classe hérite du code de l'interface",
      "La classe s'engage à respecter la structure de l'interface (avoir les propriétés et méthodes déclarées)",
      "L'interface devient une instance de la classe",
      "La classe est convertie automatiquement en interface"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Une classe peut **implémenter** une interface avec `implements`. Cela signifie que la classe doit fournir toutes les propriétés et méthodes déclarées dans l'interface. Si une propriété ou méthode manque, TypeScript génère une erreur. L'interface ne fournit pas d'implémentation (contrairement à l'héritage de classe avec `extends`). C'est un contrat que la classe doit respecter."
  }
]