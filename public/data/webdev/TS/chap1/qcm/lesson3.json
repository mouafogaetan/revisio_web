[
  {
    "questionText": "Quelle est la commande correcte pour compiler un fichier <code>index.ts</code> avec TypeScript ?",
    "options": [
      "ts-node index.ts",
      "tsc index.ts",
      "node index.ts",
      "typescript index.ts"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "La commande `tsc index.ts` utilise le compilateur TypeScript (tsc) pour transpiler le fichier TypeScript en JavaScript. `ts-node` exécute directement du TS sans compilation préalable, `node` exécute du JS uniquement."
  },
  {
    "questionText": "Comment typer les paramètres et la valeur de retour d'une fonction qui additionne deux nombres ?",
    "options": [
      "function add(a: any, b: any): any { return a + b; }",
      "function add(a: string, b: string): string { return a + b; }",
      "function add(a: number, b: number): number { return a + b; }",
      "function add(a, b) { return a + b; }"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La bonne syntaxe TypeScript pour typer une fonction additionnant deux nombres est `function add(a: number, b: number): number`. Les types `any` annulent la sécurité du typage, les types `string` ne sont pas adaptés à une addition mathématique."
  },
  {
    "questionText": "Que se passe-t-il si on exécute <code>tsc --noEmitOnError fichier.ts</code> et que le fichier contient une erreur de type ?",
    "options": [
      "Le JavaScript est généré normalement malgré l'erreur",
      "Aucun fichier JavaScript n'est généré",
      "La compilation plante sans aucun message d'erreur",
      "Le compilateur supprime automatiquement l'erreur"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Avec l'option `--noEmitOnError`, le compilateur n'émet pas de fichier JavaScript si au moins une erreur de type est détectée. Par défaut (sans cette option), TypeScript génère quand même le JS même en présence d'erreurs."
  },
  {
    "questionText": "Quelle option de <code>tsconfig.json</code> permet de spécifier le dossier de sortie des fichiers JavaScript compilés ?",
    "options": [
      "rootDir",
      "module",
      "target",
      "outDir"
    ],
    "correctAnswer": 3,
    "difficulty": "easy",
    "explanation": "`outDir` indique le dossier où seront placés les fichiers JavaScript générés. `rootDir` spécifie le dossier racine des fichiers source TypeScript, `target` la version ECMAScript cible, `module` le système de modules."
  },
  {
    "questionText": "Observez ce code TypeScript :\n&lt;pre&gt;<code>function multiply(a: number, b?: number): number {\n  if (b === undefined) return a * a;\n  return a * b;\n}</code>&lt;/pre&gt;\nQue se passe-t-il si on appelle <code>multiply(5)</code> ?",
    "options": [
      "Erreur de compilation : paramètre manquant",
      "Retourne 25 (5 × 5)",
      "Retourne NaN",
      "Retourne undefined"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Le paramètre `b` est optionnel (indiqué par `?`). S'il n'est pas fourni, la fonction retourne `a * a` (le carré). Donc `multiply(5)` retourne 25."
  },
  {
    "questionText": "Quel est l'effet de l'option de compilation <code>--watch</code> (ou `-w`) ?",
    "options": [
      "Elle surveille les fichiers et recompile automatiquement à chaque modification",
      "Elle affiche le temps d'exécution de la compilation",
      "Elle empêche la génération de fichiers source maps",
      "Elle réduit la taille du code généré"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "L'option `--watch` (ou `-w`) place le compilateur en mode \"surveillance\" : il reste actif et recompile automatiquement les fichiers TypeScript dès qu'ils sont modifiés et sauvegardés."
  },
  {
    "questionText": "Soit la fonction suivante :\n&lt;pre&gt;<code>function greet(name: string, age: number = 18): string {\n  return `${name} a ${age} ans`;\n}</code>&lt;/pre&gt;\nQuels appels sont valides en TypeScript ?",
    "options": [
      "Uniquement greet('Alice', 25)",
      "Uniquement greet('Alice')",
      "greet('Alice') et greet('Alice', 25)",
      "greet('Alice', 'vingt')"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "Le paramètre `age` a une valeur par défaut (18), il est donc optionnel. On peut l'omettre (`greet('Alice')` utilise 18) ou le fournir (`greet('Alice', 25)`). `greet('Alice', 'vingt')` est invalide car le second paramètre doit être un nombre."
  },
  {
    "questionText": "Quelle configuration dans <code>tsconfig.json</code> permet de compiler vers ES2020 et d'utiliser les modules CommonJS ?",
    "options": [
      "{ \"compilerOptions\": { \"target\": \"ES2020\", \"module\": \"CommonJS\" } }",
      "{ \"compilerOptions\": { \"esVersion\": \"ES2020\", \"moduleSystem\": \"CommonJS\" } }",
      "{ \"compiler\": { \"target\": \"ES2020\", \"modules\": \"CommonJS\" } }",
      "{ \"options\": { \"target\": \"ES2020\", \"module\": \"CommonJS\" } }"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "Dans `tsconfig.json`, les options du compilateur sont placées sous `compilerOptions`. `target` définit la version ECMAScript de sortie, `module` le système de modules (CommonJS, ESNext, etc.)."
  },
  {
    "questionText": "Que signifie l'erreur TypeScript suivante : <code>Parameter 'x' implicitly has an 'any' type</code> ?",
    "options": [
      "Le paramètre x n'a pas de type déclaré et `noImplicitAny` est activé",
      "Le type de x est automatiquement 'number'",
      "La fonction ne retourne rien",
      "Il manque un point-virgule"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "Quand `noImplicitAny` est activé (inclus dans `strict`), TypeScript interdit les paramètres sans type explicite. Cette erreur indique qu'il faut typer `x` ou désactiver l'option (déconseillé)."
  },
  {
    "questionText": "Avec ce fichier <code>tsconfig.json</code> partiel :\n&lt;pre&gt;<code>{\n  \"compilerOptions\": {\n    \"rootDir\": \"./src\",\n    \"outDir\": \"./dist\"\n  }\n}</code>&lt;/pre&gt;\nOù le compilateur cherchera-t-il les fichiers <code>.ts</code> ?",
    "options": [
      "À la racine du projet",
      "Dans le dossier ./src",
      "Dans le dossier ./dist",
      "Partout où il trouve des fichiers .ts"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`rootDir` spécifie le dossier racine contenant les fichiers TypeScript source. Le compilateur cherchera les fichiers dans `./src`. Les fichiers générés iront dans `./dist` (défini par `outDir`)."
  }
]