[
  {
    "intitule": "Maîtrise des requêtes HTTP avec Flutter",
    "type": "ressource",
    "niveau": "medium",
    "enonce": {
      "texte": "<p>Vous développez une application météo en Flutter. Pour obtenir les données, vous devez interroger une API REST. En vous basant sur vos connaissances du package <code>http</code>, répondez aux questions suivantes.</p>",
      "images": [
        "Diagramme d'une application Flutter envoyant une requête GET à un serveur API et recevant une réponse JSON",
        "Capture d'écran du fichier pubspec.yaml avec la dépendance http: ^1.1.0"
      ]
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Expliquez la procédure complète pour intégrer et utiliser le package 'http' dans un projet Flutter afin d'effectuer une requête GET vers 'https://api.meteo.com/temps'. N'oubliez pas la gestion des erreurs.",
        "niveau": "hard",
        "reponse": "1. Ajouter 'http: ^1.1.0' dans la section 'dependencies' du fichier 'pubspec.yaml'. 2. Importer le package avec 'import 'package:http/http.dart' as http;'. 3. Écrire une fonction async (ex: 'Future&lt;Map&lt;String, dynamic&gt;&gt; fetchMeteo() async') qui: (a) utilise 'try-catch', (b) exécute 'final response = await http.get(Uri.parse('https://api.meteo.com/temps'));', (c) vérifie 'if (response.statusCode == 200)' (succès) puis décode 'jsonDecode(response.body)', (d) dans le 'else', lève une exception ou gère les autres codes (404, 500), (e) dans le 'catch', gère les exceptions réseau ou de décodage.",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Application de suivi des prix",
    "type": "competence",
    "niveau": "hard",
    "enonce": {
      "texte": "<p>Vous êtes chargé(e) de créer une fonctionnalité pour une application de e-commerce. Cette fonctionnalité doit ajouter un nouveau produit au panier en utilisant une requête HTTP POST sur l'URL 'https://api.ma-boutique.com/panier/ajouter'. Les données à envoyer sont au format JSON : <code>{\"productId\": 123, \"quantite\": 2}</code>.</p>",
      "images": [
        "Schéma illustrant une application mobile envoyant une requête POST avec un corps JSON à un serveur.",
        "Icônes représentant les codes de statut HTTP 200 (succès) et 400 (erreur client)."
      ]
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Écrivez une fonction asynchrone 'ajouterAuPanier' en Dart/Flutter qui envoie les données produit au serveur. Gérez les cas de succès (statut 200 ou 201) et les erreurs (statut 400 ou exception réseau).",
        "niveau": "hard",
        "reponse": "```dart\nFuture&lt;bool&gt; ajouterAuPanier(int productId, int quantite) async {\n  final url = Uri.parse('https://api.ma-boutique.com/panier/ajouter');\n  final body = jsonEncode({'productId': productId, 'quantite': quantite});\n\n  try {\n    final response = await http.post(\n      url,\n      headers: {'Content-Type': 'application/json'},\n      body: body,\n    );\n\n    if (response.statusCode == 200 || response.statusCode == 201) {\n      // Succès\n      print('Produit ajouté avec succès');\n      return true;\n    } else {\n      // Erreur HTTP\n      print('Erreur serveur : ${response.statusCode} - ${response.body}');\n      return false;\n    }\n  } catch (e) {\n    // Exception réseau ou autre\n    print('Erreur réseau ou d'envoi : $e');\n    return false;\n  }\n}\n```",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Identification et correction d'erreurs",
    "type": "ressource",
    "niveau": "medium",
    "enonce": {
      "texte": "<p>Un développeur junior a écrit le code suivant pour récupérer une liste d'utilisateurs. Analysez ce code, identifiez TOUTES les erreurs (conceptuelles et de syntaxe) et proposez une version corrigée.</p>\n&lt;pre&gt;<code>import 'package:http/http.dart';\n\nvoid fetchUsers() {\n  final response = http.get('https://api.example.com/users');\n  if (response.statusCode == 200) {\n    var data = jsonDecode(response.body);\n    print(data);\n  }\n}</code>&lt;/pre&gt;",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Listez toutes les erreurs présentes dans le code fourni. Justifiez brièvement chaque erreur.",
        "niveau": "medium",
        "reponse": "1. **Import incorrect** : 'import 'package:http/http.dart'' importe la bibliothèque mais sans alias, l'utilisation de 'http.get' nécessite l'alias 'as http' ou une importation préfixée. 2. **Fonction non asynchrone** : 'get' est une opération asynchrone, la fonction doit être marquée 'async' et utiliser 'await'. 3. **URL non encodée** : 'http.get' attend un objet 'Uri', pas une String. Il faut utiliser 'Uri.parse()'. 4. **Absence de 'await'** : 'response' recevra un 'Future' et non le résultat. 5. **Absence de gestion d'erreur** : Pas de bloc 'try-catch' pour les exceptions réseau. 6. **Manque de vérification du 'statusCode'** : On suppose un succès (200) mais il faut gérer les autres cas. 7. **'jsonDecode' non importé** : La fonction nécessite 'import 'dart:convert';'.",
        "sousQuestions": [
          {
            "numero": "1.a",
            "texte": "Réécrivez le code complètement corrigé, en y ajoutant une gestion robuste des erreurs.",
            "niveau": "hard",
            "reponse": "```dart\nimport 'dart:convert';\nimport 'package:http/http.dart' as http;\n\nFuture&lt;void&gt; fetchUsers() async {\n  final url = Uri.parse('https://api.example.com/users');\n  try {\n    final response = await http.get(url);\n    if (response.statusCode == 200) {\n      var data = jsonDecode(response.body);\n      print('Données reçues : $data');\n    } else {\n      print('Erreur HTTP : ${response.statusCode}');\n    }\n  } catch (e) {\n    print('Une erreur est survenue : $e');\n  }\n}\n```"
          }
        ]
      }
    ]
  },
  {
    "intitule": "Création d'un service météo",
    "type": "competence",
    "niveau": "very hard",
    "enonce": {
      "texte": "<p>Vous devez créer un service météo complet. L'API 'https://api.open-meteo.com/v1/forecast?latitude=3.87&longitude=11.52&current_weather=true' retourne la météo pour Yaoundé. En utilisant le package <code>http</code>, concevez un service qui récupère, parse et expose la température actuelle.</p>",
      "images": [
        "Capture de la réponse JSON de l'API Open-Meteo montrant la structure avec 'current_weather' et 'temperature'.",
        "Schéma d'architecture avec un 'WeatherService' (classe Dart) comme couche intermédiaire entre l'UI et l'API."
      ]
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Créez une classe 'WeatherService' avec une méthode 'getCurrentTemperature(double latitude, double longitude)' qui: (1) construit l'URL, (2) exécute la requête GET, (3) parse la réponse JSON, et (4) retourne la température (double). La méthode doit gérer les erreurs et retourner 'null' en cas d'échec.",
        "niveau": "very hard",
        "reponse": "```dart\nimport 'dart:convert';\nimport 'package:http/http.dart' as http;\n\nclass WeatherService {\n  final String baseUrl = 'https://api.open-meteo.com/v1/forecast';\n\n  Future&lt;double?&gt; getCurrentTemperature(double latitude, double longitude) async {\n    final url = Uri.parse('$baseUrl?latitude=$latitude&longitude=$longitude&current_weather=true');\n\n    try {\n      final response = await http.get(url);\n      if (response.statusCode == 200) {\n        final Map&lt;String, dynamic&gt; data = jsonDecode(response.body);\n        if (data.containsKey('current_weather') && data['current_weather'] != null) {\n          final currentWeather = data['current_weather'] as Map&lt;String, dynamic&gt;;\n          if (currentWeather.containsKey('temperature')) {\n            return currentWeather['temperature'] as double;\n          }\n        }\n        return null; // Structure JSON inattendue\n      } else {\n        print('Erreur HTTP : ${response.statusCode}');\n        return null;\n      }\n    } catch (e) {\n      print('Exception lors de l'appel API : $e');\n      return null;\n    }\n  }\n}\n```",
        "sousQuestions": []
      }
    ]
  }
]