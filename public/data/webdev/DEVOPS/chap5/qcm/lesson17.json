[
  {
    "questionText": "Dans un Dockerfile, quelle instruction est utilisée pour définir la commande par défaut qui sera exécutée lorsqu'un conteneur est lancé, tout en permettant de la surcharger via la ligne de commande ?",
    "options": [
      "`ENTRYPOINT`",
      "`CMD`",
      "`RUN`",
      "`CMD` et `ENTRYPOINT` ensemble uniquement"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "`CMD` définit la commande par défaut. Elle peut être remplacée si l'utilisateur en fournit une autre au moment du `docker run`. `ENTRYPOINT` est plus rigide. `RUN` exécute des commandes pendant la construction. L'association des deux existe (ENTRYPOINT pour l'exécutable + CMD pour les paramètres par défaut), mais la question demande spécifiquement l'instruction qui **permet d'être facilement surchargée** → c'est `CMD`."
  },
  {
    "questionText": "Vous travaillez à Douala avec une connexion Internet limitée. Vous construisez fréquemment une image Docker pour une application Node.js. Quelle pratique permet de réduire à la fois le temps de build et la consommation de data ?",
    "options": [
      "Toujours utiliser `--no-cache`",
      "Copier uniquement le fichier `package.json` d'abord, exécuter `npm install`, puis copier le reste du code",
      "Utiliser une seule couche avec `RUN npm install && npm cache clean --force`",
      "Ne pas utiliser de `.dockerignore`"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Copier d'abord `package.json` et exécuter `npm install` permet de mettre en cache cette couche. Tant que `package.json` ne change pas, Docker réutilise le cache des dépendances, évitant de retélécharger les paquets npm à chaque build. C'est crucial avec une connexion limitée. L'option `--no-cache` ferait l'inverse (forcer le re-téléchargement). `.dockerignore` est utile mais n'impacte pas directement le cache des dépendances."
  },
  {
    "questionText": "Quel est l'avantage principal d'une construction multi-stage dans un Dockerfile ?",
    "options": [
      "Augmenter la sécurité en isolant chaque instruction",
      "Permettre d'exécuter plusieurs commandes `CMD`",
      "Réduire considérablement la taille finale de l'image en ne conservant que les artefacts nécessaires",
      "Accélérer le démarrage des conteneurs"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "Le multi-stage permet d'utiliser un premier stage avec tous les outils de build (compilateurs, SDK, etc.) puis un deuxième stage plus léger (ex: `alpine`) dans lequel on ne copie que l'exécutable final. L'image finale est ainsi beaucoup plus petite, plus rapide à télécharger (utile au Cameroun) et plus sécurisée (moins d'outils inutiles)."
  },
  {
    "questionText": "Dans un Dockerfile, que fait l'instruction `COPY --chown=www-data:www-data . /app` ?",
    "options": [
      "Elle copie le contenu du répertoire courant vers `/app` et attribue la propriété à l'utilisateur `www-data`",
      "Elle change le propriétaire de l'image Docker entière",
      "Elle crée un utilisateur `www-data` dans le conteneur",
      "Elle copie uniquement les fichiers appartenant à `www-data` sur l'hôte"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "L'option `--chown` dans `COPY` change l'utilisateur et le groupe propriétaire des fichiers **dans le conteneur** après la copie. C'est une bonne pratique pour ne pas exécuter les applications en tant que `root` (sécurité). `www-data` est un utilisateur standard pour les serveurs web."
  },
  {
    "questionText": "Pourquoi est-il recommandé d'utiliser un fichier `.dockerignore` même pour un petit projet ?",
    "options": [
      "Pour chiffrer les fichiers avant de les envoyer au daemon Docker",
      "Pour empêcher l'envoi de fichiers inutiles (`.git/`, `node_modules/`, fichiers temporaires) au contexte de build, accélérant le build et réduisant la taille",
      "C'est obligatoire, Docker refuse de builder sans `.dockerignore`",
      "Pour définir des variables d'environnement par défaut"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Le contexte de build (envoyé au démon Docker) inclut tous les fichiers du répertoire courant. Sans `.dockerignore`, vous envoyez des dossiers lourds comme `node_modules` (si présent côté hôte) ou l'historique Git. Cela ralentit le build (surtout avec une connexion 4G limitée à Yaoundé ou Douala) et peut créer des images plus grosses."
  },
  {
    "questionText": "Vous avez un Dockerfile avec ces deux dernières lignes :\n```dockerfile\nCOPY script.sh /script.sh\nENTRYPOINT [\"/script.sh\"]\n```\nQue se passe-t-il si vous exécutez : `docker run monimage --help` ?",
    "options": [
      "Le conteneur échoue car `--help` n'est pas compris",
      "Le script `/script.sh` est exécuté avec `--help` comme argument",
      "Docker ignore `--help` car `ENTRYPOINT` ne prend pas d'arguments",
      "La commande `--help` remplace `ENTRYPOINT` et est exécutée directement"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Avec `ENTRYPOINT`, tout ce qui est écrit après `docker run` (ici `--help`) est passé **en argument** à l'entrypoint. C'est différent de `CMD`. Si on avait utilisé `CMD [\"/script.sh\"]`, alors `--help` aurait remplacé `/script.sh`. C'est un piège classique en DevOps."
  },
  {
    "questionText": "Quelle instruction utiliser pour exécuter une commande lors de la construction de l'image (par exemple, mettre à jour les paquets Linux) et dont le résultat sera figé dans l'image ?",
    "options": [
      "`ENTRYPOINT`",
      "`CMD`",
      "`RUN`",
      "`ADD`"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "`RUN` exécute des commandes **au moment du build** (`docker build`). Les modifications (ex: `RUN apt-get update && apt-get install -y curl`) sont enregistrées dans la couche de l'image. `CMD` et `ENTRYPOINT` s'exécutent au démarrage du conteneur. `ADD` sert à copier des fichiers."
  },
  {
    "questionText": "Dans un pays où l'électricité peut être instable, vous devez souvent relancer des builds. Pour optimiser la reprise, il faut :",
    "options": [
      "Tout mettre dans une seule instruction `RUN`",
      "Mettre les instructions qui changent rarement (comme l'installation de dépendances système) en haut du Dockerfile",
      "Toujours utiliser `--squash`",
      "Ne jamais utiliser le cache Docker"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Docker construit couche par couche et met en cache chaque instruction réussie. Si vous placez `RUN apt-get update ...` en haut (avant de copier votre code qui change souvent), cette couche sera réutilisée même si vous modifiez votre code. En cas de coupure électrique au moment du build, la reprise sera plus rapide car les premières couches sont déjà construites."
  },
  {
    "questionText": "Quelle est la différence entre `COPY` et `ADD` dans un Dockerfile ?",
    "options": [
      "`COPY` ne peut copier que des fichiers texte, `ADD` peut copier des binaires",
      "`ADD` supporte automatiquement les URLs et l'extraction d'archives tar, ce qui peut être indésirable. `COPY` est plus simple et prévisible",
      "`COPY` est obsolète, il faut toujours utiliser `ADD`",
      "`ADD` ne fonctionne que pour les répertoires, `COPY` pour les fichiers"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "`ADD` a des 'magies' : décompression automatique d'archives et téléchargement depuis URLs. Cela peut causer des comportements inattendus. **La bonne pratique** est d'utiliser `COPY` pour copier des fichiers/dossiers locaux et `RUN curl` ou `wget` pour télécharger des fichiers, afin de garder le Dockerfile explicite."
  },
  {
    "questionText": "L'instruction `WORKDIR /app` dans un Dockerfile a quel effet ?",
    "options": [
      "Elle crée un volume persistant nommé `/app`",
      "Elle définit le répertoire de travail pour les instructions `RUN`, `CMD`, `ENTRYPOINT`, `COPY` et `ADD` qui suivent",
      "Elle partage le dossier `/app` avec l'hôte",
      "Elle définit la variable d'environnement `$WORKDIR` à `/app`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "`WORKDIR` change le répertoire courant dans le conteneur pour toutes les instructions suivantes. Si `/app` n'existe pas, il est créé. Cela évite d'écrire `RUN cd /app && ...` partout. C'est une bonne pratique pour organiser son Dockerfile."
  }
]