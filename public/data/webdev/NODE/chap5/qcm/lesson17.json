[
  {
    "questionText": "Quelle commande npm permet d'installer simultanément Sequelize, le pilote PostgreSQL (`pg`) et `pg-hstore` ?",
    "options": [
      "`npm install sequelize`",
      "`npm install pg pg-hstore`",
      "`npm install sequelize pg pg-hstore`",
      "`npm install --save-dev sequelize pg`"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Pour utiliser Sequelize avec PostgreSQL, vous devez installer le package `sequelize` lui-même ainsi que le pilote de base de données (`pg`) et `pg-hstore` (pour la sérialisation des données JSONB). La commande `npm install sequelize pg pg-hstore` est donc la bonne."
  },
  {
    "questionText": "Dans un modèle Sequelize, comment définir une colonne `email` de type `STRING` qui ne peut pas être nulle et dont la valeur doit être unique ?",
    "options": [
      "`email: { type: DataTypes.STRING }`",
      "`email: { type: DataTypes.STRING, allowNull: false }`",
      "`email: { type: DataTypes.STRING, unique: true }`",
      "`email: { type: DataTypes.STRING, allowNull: false, unique: true }`"
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "L'option `allowNull: false` rend la colonne obligatoire (NOT NULL), tandis que `unique: true` assure qu'aucun enregistrement ne peut avoir la même valeur d'email. Les deux doivent être combinés pour respecter les contraintes décrites."
  },
  {
    "questionText": "Soit les modèles `User` et `Profile`. Un `User` possède **un et un seul** `Profile`, et un `Profile` appartient à **un seul** `User`. Quelle est l'association correcte à définir dans Sequelize ?",
    "options": [
      "`User.hasMany(Profile)` et `Profile.belongsTo(User)`",
      "`User.hasOne(Profile)` et `Profile.belongsTo(User)`",
      "`User.belongsTo(Profile)` et `Profile.hasOne(User)`",
      "`User.hasOne(Profile)` et `Profile.hasOne(User)`"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "La relation **un-à-un** se gère avec `hasOne` (côté parent) et `belongsTo` (côté enfant). Ici, `User` est le parent (il 'possède' un profile) donc `User.hasOne(Profile)`. Inversement, `Profile` appartient à un `User` donc `Profile.belongsTo(User)`. C'est le pattern standard pour ce type de relation."
  },
  {
    "questionText": "Quelle méthode Sequelize utiliseriez-vous pour trouver tous les utilisateurs dont le `nom` commence par \"Kam\" et qui sont **actifs** (`isActive: true`), en incluant leurs profils associés ?",
    "options": [
      "`User.findAll({ where: { nom: { [Op.startsWith]: 'Kam' }, isActive: true }, include: [Profile] })`",
      "`User.findAndCountAll({ nom: 'Kam%', isActive: true, with: 'Profile' })`",
      "`User.query('SELECT * FROM Users WHERE nom LIKE $1 AND isActive = $2', ['Kam%', true])`",
      "`User.get({ where: { nom: /^Kam/, isActive: 1 }, relations: ['Profile'] })`"
    ],
    "correctAnswer": 0,
    "difficulty": "very hard",
    "explanation": "Sequelize utilise l'opérateur `[Op.startsWith]` (issu de `Sequelize.Op`) pour générer la clause SQL `LIKE 'Kam%'`. L'option `where` filtre les utilisateurs, et `include: [Profile]` permet d'effectuer un `JOIN` ou une requête séparée pour charger les profils associés (grace à l'association `hasOne`/`belongsTo`)."
  },
  {
    "questionText": "Vous avez défini une association `User.hasMany(Post)`. Quel code Sequelize permet de créer un nouveau `Post` pour un `User` existant ?",
    "options": [
      "`Post.create({ userId: user.id, title: '...' })`",
      "`user.createPost({ title: '...' })`",
      "`User.createPost({ userId: user.id, title: '...' })`",
      "`Post.build({ title: '...', user: user })`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Lorsqu'une association `hasMany` (ou `hasOne`) est définie, Sequelize ajoute automatiquement des **méthodes magiques** aux instances. Ici, `user.createPost()` est injecté et s'occupe de remplir automatiquement la clé étrangère (`userId`) avec l'id de `user`."
  },
  {
    "questionText": "Quelle est l'utilité de l'option `hooks: true` dans la méthode `bulkCreate()` de Sequelize ?",
    "options": [
      "Elle permet de créer plusieurs enregistrements en une seule requête SQL.",
      "Elle ignore les validations définies dans le modèle.",
      "Elle exécute les hooks (comme `beforeCreate`, `afterCreate`) pour chaque ligne insérée.",
      "Elle met à jour les enregistrements existants au lieu de lever une erreur de duplication."
    ],
    "correctAnswer": 2,
    "difficulty": "hard",
    "explanation": "Par défaut, `bulkCreate` n'exécute **pas** les hooks individuels des modèles pour des raisons de performance. En passant `{ hooks: true }`, vous forcez Sequelize à déclencher les hooks (ex: validation, hashage de mot de passe) sur chaque élément du tableau, ce qui est plus lent mais plus sûr."
  },
  {
    "questionText": "Quelle commande Sequelize (CLI) est utilisée pour créer un fichier de migration vide après avoir installé `sequelize-cli` ?",
    "options": [
      "`sequelize migration:generate --name nom-de-la-migration`",
      "`npx sequelize-cli migration:create --name nom-de-la-migration`",
      "`sequelize make:migration nom_de_la_migration`",
      "`npx sequelize db:migrate:create nom_de_la_migration`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Avec la CLI officielle, la commande pour **générer** un squelette de migration est `npx sequelize-cli migration:generate --name &lt;nom&gt;` (ou `migration:create` selon les versions récentes). `db:migrate` sert à exécuter les migrations, pas à les créer."
  },
  {
    "questionText": "Comment instancier un objet `Sequelize` pour se connecter à une base PostgreSQL nommée `ecole_db` sur `localhost` avec l'utilisateur `admin` et le mot de passe `secret` ?",
    "options": [
      "`new Sequelize('postgres://admin:secret@localhost:5432/ecole_db');`",
      "`new Sequelize('ecole_db', 'admin', 'secret', { host: 'localhost', dialect: 'postgres' });`",
      "`new Sequelize({ database: 'ecole_db', username: 'admin', password: 'secret', host: 'localhost', dialect: 'postgres' });`",
      "Toutes ces réponses sont valides."
    ],
    "correctAnswer": 3,
    "difficulty": "medium",
    "explanation": "Sequelize accepte plusieurs signatures pour le constructeur : une URI de connexion, des paramètres séparés (database, username, password, options), ou un unique objet de configuration. Les trois syntaxes présentées sont donc correctes et équivalentes."
  },
  {
    "questionText": "Soit un modèle `Product`. Quelle requête Sequelize permet de mettre à jour le `prix` de tous les produits dont la `categorie` est \"électronique\" en y ajoutant 5 ?",
    "options": [
      "`Product.update({ prix: prix + 5 }, { where: { categorie: 'électronique' } })`",
      "`Product.increment('prix', { by: 5, where: { categorie: 'électronique' } })`",
      "`Product.updateAll({ prix: 'prix + 5' }, { categorie: 'électronique' })`",
      "`Product.add({ prix: 5 }, { where: { categorie: 'électronique' } })`"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Pour incrémenter une colonne de manière atomique (sans risque de race condition), Sequelize fournit la méthode `increment()`. Elle évite de lire la valeur actuelle, la modifier en JS, puis la réécrire. `update` ne peut pas référencer directement l'ancienne valeur de la colonne."
  },
  {
    "questionText": "Quelle méthode utilise-t-on pour détruire un enregistrement après l'avoir trouvé avec `findByPk` ?",
    "options": [
      "`user.destroy()`",
      "`User.delete(user.id)`",
      "`user.remove()`",
      "`User.destroy({ where: { id: user.id } })`"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "Une fois qu'une instance d'un modèle est récupérée (ex: `const user = await User.findByPk(1)`), vous pouvez appeler directement la méthode **d'instance** `destroy()` sur cette instance. Les autres syntaxes sont valides (sauf `user.remove()` qui sert pour les associations), mais la plus directe et lisible est `user.destroy()`."
  }
]