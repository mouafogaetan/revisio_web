[
  {
    "intitule": "Identifier les erreurs de style avec ESLint",
    "type": "ressource",
    "niveau": "easy",
    "enonce": {
      "texte": "<p>Voici un extrait de code JavaScript contenant plusieurs erreurs que ESLint (avec la configuration recommandée) détecterait. Identifiez chaque erreur en vous basant sur les règles courantes (indentation, variables non utilisées, guillemets, points-virgules, etc.).</p><pre><code>function add(a,b){\nlet result = a+b\nconsole.log('La somme est '+result)\nvar unusedVar = 42;\nreturn result\n}</code></pre><p>Rappel : ESLint aide à maintenir un style cohérent et à éviter les bugs silencieux.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Listez au moins 4 problèmes de style ou de bonnes pratiques que ESLint signalerait dans ce code. Justifiez chaque problème en vous référant aux règles courantes (ex: semi, no-unused-vars, quotes, space-before-function-paren, etc.).",
        "niveau": "easy",
        "reponse": "1. Manque d'espace après la virgule dans les paramètres : `(a,b)` → devrait être `(a, b)` (règle `comma-spacing`).\n2. Absence de point-virgule après `let result = a+b` (règle `semi`).\n3. Utilisation de `var` au lieu de `let` ou `const` (règle `no-var`).\n4. Variable `unusedVar` déclarée mais jamais utilisée (règle `no-unused-vars`).\n5. Utilisation de guillemets simples alors que la chaîne contient une apostrophe ? Non, ici les guillemets simples sont corrects mais on pourrait imposer des doubles (règle `quotes`).\n6. Manque de point-virgule après `return result` (règle `semi`).",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Configurer Prettier pour uniformiser le formatage",
    "type": "competence",
    "niveau": "medium",
    "enonce": {
      "texte": "<p>Dans une équipe de 5 développeurs, chacun utilise son propre style d'écriture (indentation, guillemets, virgules finales, etc.). Vous devez mettre en place <strong>Prettier</strong> pour automatiser le formatage et garantir un code homogène.</p><p>Le code actuel d’un fichier est le suivant :</p><pre><code>const user={name:\"Alice\",age:30, city:\"Douala\"};\nfunction greet(user){console.log('Bonjour '+user.name);}</code></pre><p>Vous devez créer un fichier de configuration <code>.prettierrc</code> et expliquer comment l’intégrer dans le flux de travail (package.json, édition, CI).</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Proposez un fichier `.prettierrc` (au format JSON) qui applique : une largeur de ligne de 80, des guillemets simples, des points-virgules obligatoires, des virgules finales dans les objets/tableaux, et une indentation de 2 espaces. Expliquez chaque option.",
        "niveau": "medium",
        "reponse": "```json\n{\n  \"printWidth\": 80,\n  \"singleQuote\": true,\n  \"semi\": true,\n  \"trailingComma\": \"all\",\n  \"tabWidth\": 2\n}\n```\n- `printWidth` : longueur max avant retour à la ligne.\n- `singleQuote` : utilise des guillemets simples.\n- `semi` : ajoute des points-virgules à la fin des instructions.\n- `trailingComma` : ajoute une virgule après le dernier élément (plus propre pour git).\n- `tabWidth` : nombre d'espaces par niveau d'indentation.",
        "sousQuestions": [
          {
            "numero": "1.a",
            "texte": "Appliquez mentalement ces règles au code fourni. Réécrivez le code formaté correctement par Prettier.",
            "niveau": "easy",
            "reponse": "```javascript\nconst user = {\n  name: 'Alice',\n  age: 30,\n  city: 'Douala',\n};\nfunction greet(user) {\n  console.log('Bonjour ' + user.name);\n}\n```"
          },
          {
            "numero": "1.b",
            "texte": "Quelle commande npm doit-on ajouter dans `package.json` (section scripts) pour formater tout le code source du dossier `src/` ?",
            "niveau": "easy",
            "reponse": "`\"format\": \"prettier --write \\\"src/**/*.js\\\"\"`\nOn peut aussi utiliser `\"format\": \"prettier --write .\"` pour tout le projet."
          }
        ]
      }
    ]
  },
  {
    "intitule": "Automatiser le linting et le formatage avant chaque commit avec Husky",
    "type": "competence",
    "niveau": "hard",
    "enonce": {
      "texte": "<p>Vous travaillez sur un projet Node.js où la qualité du code est essentielle. Vous décidez d’imposer l’exécution d’ESLint et de Prettier avant chaque commit, grâce à <strong>Husky</strong> et <strong>lint-staged</strong>. Si le code ne respecte pas les règles, le commit est rejeté.</p><p>Décrivez les étapes d’installation et de configuration, en donnant les commandes et les extraits de fichiers nécessaires.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Détaillez la procédure complète : installation des dépendances, initialisation de Husky, création du hook pre-commit, configuration de lint-staged. Fournissez les commandes bash et les modifications des fichiers (`package.json`, `.husky/pre-commit`).",
        "niveau": "hard",
        "reponse": "**Étapes :**\n1. Installer Husky et lint-staged : `npm install --save-dev husky lint-staged`\n2. Activer les hooks Git : `npx husky install`\n3. Ajouter un script `prepare` dans `package.json` : `\"prepare\": \"husky install\"` (pour que les hooks soient installés après `npm install`).\n4. Configurer `lint-staged` dans `package.json` :\n```json\n\"lint-staged\": {\n  \"*.js\": [\"eslint --fix\", \"prettier --write\", \"git add\"]\n}\n```\n5. Créer le hook pre-commit : `npx husky add .husky/pre-commit \"npx lint-staged\"`\n6. (Optionnel) Ajouter un message informatif.\n\nRésultat : à chaque `git commit`, lint-staged vérifie les fichiers .js, exécute eslint --fix et prettier --write, puis les ajoute automatiquement. Si une erreur persiste, le commit échoue.",
        "sousQuestions": [
          {
            "numero": "1.a",
            "texte": "Expliquez pourquoi il est préférable d’utiliser `lint-staged` plutôt que de linter tout le projet à chaque commit.",
            "niveau": "medium",
            "reponse": "`lint-staged` n’applique les vérifications qu’aux fichiers modifiés dans le commit (ceux en stage). Cela évite de passer des heures à corriger l’ensemble du code legacy et accélère le processus de commit, surtout sur les grands projets."
          }
        ]
      }
    ]
  },
  {
    "intitule": "Analyser un rapport ESLint et proposer des corrections",
    "type": "ressource",
    "niveau": "medium",
    "enonce": {
      "texte": "<p>Après avoir exécuté `npx eslint src/ --format json`, vous obtenez le rapport simplifié suivant pour un fichier `auth.js` :</p><pre><code>{\n  \"filePath\": \"src/auth.js\",\n  \"messages\": [\n    {\"ruleId\": \"no-unused-vars\", \"line\": 3, \"column\": 7, \"message\": \"'token' is defined but never used.\"},\n    {\"ruleId\": \"eqeqeq\", \"line\": 8, \"column\": 12, \"message\": \"Expected '===' and instead saw '=='.\"},\n    {\"ruleId\": \"no-console\", \"line\": 12, \"column\": 3, \"message\": \"Unexpected console statement.\"}\n  ]\n}</code></pre><p>Le code source correspondant (lignes indicatives) :</p><pre><code>1  function verifyUser(token, user) {\n2    let isValid = false;\n3    if (user.role == 'admin') isValid = true;\n4    console.log('Vérification effectuée');\n5    return isValid;\n6  }</code></pre>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Pour chaque erreur signalée, expliquez la règle violée, pourquoi elle est importante, et corrigez le code en conséquence.",
        "niveau": "medium",
        "reponse": "- **no-unused-vars** : la variable `token` n’est jamais utilisée. Règle importante pour éviter le code mort. Correction : supprimer `token` des paramètres ou l’utiliser.\n- **eqeqeq** : utilisation de `==` au lieu de `===` peut causer des comparaisons inattendues (ex: `'0' == 0` est vrai). Correction : remplacer `==` par `===`.\n- **no-console** : les `console.log` en production sont généralement interdits car ils polluent les logs. Correction : supprimer ou remplacer par un logger professionnel (ex: winston).\n\nCode corrigé :\n```javascript\nfunction verifyUser(user) {\n  let isValid = false;\n  if (user.role === 'admin') isValid = true;\n  // console.log supprimé\n  return isValid;\n}\n```",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Créer un flux d'intégration continue (CI) avec linting obligatoire",
    "type": "competence",
    "niveau": "very hard",
    "enonce": {
      "texte": "<p>Vous gérez un dépôt GitHub pour une API Node.js. Vous voulez que chaque <em>pull request</em> exécute automatiquement ESLint et Prettier (vérification seulement, pas d’écriture) avant de pouvoir fusionner. En cas d’erreur, la PR est bloquée.</p><p>Rédigez le contenu d’un fichier de workflow GitHub Actions (YAML) qui réalise cette tâche. Incluez les étapes : checkout, installation des dépendances, exécution de ESLint et vérification du formatage Prettier.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Écrivez le fichier `.github/workflows/lint.yml` complet. Expliquez chaque étape et les commandes utilisées.",
        "niveau": "very hard",
        "reponse": "```yaml\nname: Lint and Format Check\n\non:\n  pull_request:\n    branches: [ main, develop ]\n\njobs:\n  lint:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout code\n        uses: actions/checkout@v4\n\n      - name: Setup Node.js\n        uses: actions/setup-node@v4\n        with:\n          node-version: '18'\n          cache: 'npm'\n\n      - name: Install dependencies\n        run: npm ci\n\n      - name: Run ESLint\n        run: npx eslint . --max-warnings=0\n\n      - name: Check Prettier formatting\n        run: npx prettier --check .\n```\n**Explications** :\n- `on.pull_request` : déclenche le workflow pour toute PR vers les branches principales.\n- `actions/checkout` : récupère le code du dépôt.\n- `setup-node` : installe Node.js et active le cache npm.\n- `npm ci` : installation rapide et fiable basée sur le package-lock.json.\n- `eslint --max-warnings=0` : échoue si au moins une erreur OU un avertissement (strict).\n- `prettier --check` : vérifie que le code est bien formaté sans modifier les fichiers. Échoue si des différences sont trouvées.",
        "sousQuestions": []
      }
    ]
  },
  {
    "intitule": "Déboguer une configuration Husky qui ne fonctionne pas",
    "type": "competence",
    "niveau": "hard",
    "enonce": {
      "texte": "<p>Un collègue a suivi un tutoriel pour installer Husky mais le hook pre-commit ne s’exécute pas. Il a exécuté :</p><pre><code>npm install husky --save-dev\nnpx husky add .husky/pre-commit \"npm test\"</code></pre><p>Mais lorsqu’il fait `git commit`, rien ne se passe et le commit réussit même si les tests échouent. Son système est Windows (Git Bash). Il n’a pas de dossier `.husky` à la racine.</p>",
      "images": []
    },
    "questions": [
      {
        "numero": 1,
        "texte": "Identifiez les erreurs dans la démarche de votre collègue. Proposez une procédure corrective détaillée (commandes à taper, vérifications).",
        "niveau": "hard",
        "reponse": "**Erreurs identifiées :**\n1. Il n’a pas exécuté `npx husky install` après l’installation. Sans cela, les hooks ne sont pas installés dans le dossier `.git/hooks`.\n2. La commande `npx husky add` suppose que Husky est déjà installé et que le dossier `.husky` existe. Sur certaines versions, l’ajout ne crée pas automatiquement le dossier si l’installation n’est pas faite.\n3. Sous Windows avec Git Bash, il faut parfois s’assurer que Husky utilise le bon interpréteur (mais en général cela fonctionne).\n\n**Correction :**\n```bash\nnpx husky install                # initialise les hooks et crée .husky/\nnpx husky add .husky/pre-commit \"npm test\"\n# Vérifier que le fichier .husky/pre-commit est exécutable (chmod +x sur Linux/Mac, sous Windows Git gère)\n# Ajouter le script \"prepare\" dans package.json :\n# \"prepare\": \"husky install\"\n```\nIl faut aussi que le dossier `.husky` soit commité dans le dépôt pour que les autres développeurs aient les hooks automatiquement après `npm install` (grâce au script `prepare`). Enfin, tester en faisant `git commit -m \"test\"` après avoir modifié un fichier.",
        "sousQuestions": []
      }
    ]
  }
]