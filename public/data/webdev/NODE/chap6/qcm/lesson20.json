[
  {
    "questionText": "Quel est le rôle principal de la bibliothèque `jsonwebtoken` dans une API Node.js ?",
    "options": [
      "Hacher les mots de passe des utilisateurs",
      "Générer et vérifier des tokens d'authentification au format JWT",
      "Créer des sessions utilisateur côté serveur",
      "Chiffrer la communication entre le client et le serveur"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "La bibliothèque `jsonwebtoken` est spécifiquement conçue pour créer (signer) et valider (vérifier) des tokens JWT (JSON Web Token), qui sont utilisés pour l'authentification sans état (stateless)."
  },
  {
    "questionText": "Quelle méthode de `jsonwebtoken` utilise-t-on pour créer un nouveau token JWT ?",
    "options": [
      "jwt.decode()",
      "jwt.verify()",
      "jwt.sign()",
      "jwt.generate()"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La méthode `jwt.sign(payload, secretOrPrivateKey, [options])` est utilisée pour générer un token JWT signé. `jwt.verify()` sert à vérifier un token, `jwt.decode()` à décoder sans vérifier la signature."
  },
  {
    "questionText": "Dans un middleware d'authentification JWT avec Express, où trouve-t-on généralement le token envoyé par le client ?",
    "options": [
      "Dans le corps de la requête (`req.body`)",
      "Dans les paramètres de l'URL (`req.params`)",
      "Dans l'en-tête `Authorization` (`req.headers.authorization`)",
      "Dans les cookies uniquement (`req.cookies`)"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "La pratique standard consiste à envoyer le token JWT dans l'en-tête `Authorization` au format `Bearer &lt;token&gt;`. C'est une convention largement adoptée pour les API RESTful."
  },
  {
    "questionText": "Que se passe-t-il si `jwt.verify()` reçoit un token invalide (signature erronée, token expiré, etc.) ?",
    "options": [
      "La méthode retourne `null`",
      "La méthode retourne un objet vide `{}`",
      "La méthode lève une erreur (exception)",
      "La méthode retourne `false`"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "`jwt.verify()` est synchrone et lève une erreur (ex: `JsonWebTokenError`, `TokenExpiredError`) si le token n'est pas valide. Il faut donc utiliser un bloc `try/catch` ou le passer à un callback pour gérer l'erreur."
  },
  {
    "questionText": "Comment extraire le token JWT d'un en-tête `Authorization` ayant la valeur `'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'` ?",
    "options": [
      "req.headers.authorization",
      "req.headers.authorization.split(' ')[0]",
      "req.headers.authorization.split(' ')[1]",
      "req.headers.authorization.slice(7)"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "La méthode courante est `const token = req.headers.authorization.split(' ')[1];` car l'en-tête est composé du mot-clé 'Bearer' suivi d'un espace puis du token. L'index 1 permet de récupérer le token. L'index 0 donnerait 'Bearer'."
  },
  {
    "questionText": "Quelle option permet de définir la durée de validité d'un token JWT lors de sa création avec `jwt.sign()` ?",
    "options": [
      "{ expiresIn: '1h' }",
      "{ ttl: 3600 }",
      "{ validity: '1 day' }",
      "{ maxAge: 60000 }"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "L'option standard est `expiresIn`, qui accepte des chaînes comme `'60'` (secondes), `'2 days'`, `'10h'` ou un nombre en millisecondes. C'est la méthode recommandée par la bibliothèque `jsonwebtoken`."
  },
  {
    "questionText": "Quelle information un token JWT ne doit-il JAMAIS contenir pour des raisons de sécurité ?",
    "options": [
      "L'identifiant unique de l'utilisateur (`userId`)",
      "Le rôle de l'utilisateur (`role`)",
      "Le mot de passe de l'utilisateur",
      "La date d'expiration du token (`exp`)"
    ],
    "correctAnswer": 2,
    "difficulty": "hard",
    "explanation": "Un JWT est seulement signé, pas chiffré (par défaut). Le payload est encodé en Base64URL, donc lisible par n'importe qui. Il ne faut jamais y placer d'informations sensibles comme un mot de passe, un numéro de carte bancaire, etc."
  },
  {
    "questionText": "Voici un extrait de middleware. Que va-t-il retourner au client si aucun token n'est fourni ?\n\n```javascript\nfunction authenticate(req, res, next) {\n  const authHeader = req.headers.authorization;\n  if (!authHeader) {\n    return res.status(401).json({ message: 'Token manquant' });\n  }\n  next();\n}\n```",
    "options": [
      "Une réponse avec le code 200 et un message vide",
      "Une réponse avec le code 401 et le message 'Token manquant'",
      "Une réponse avec le code 403 et le message 'Accès interdit'",
      "Une erreur 500 est levée"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Le code vérifie explicitement l'existence de `authorization`. S'il est absent, il renvoie un statut HTTP 401 (Unauthorized) avec le message JSON 'Token manquant'. Le `return` empêche l'exécution de la suite."
  },
  {
    "questionText": "Pour vérifier un token JWT avec une clé secrète stockée dans `process.env.JWT_SECRET`, quel est le code correct ?",
    "options": [
      "jwt.decode(token, process.env.JWT_SECRET)",
      "jwt.sign(token, process.env.JWT_SECRET)",
      "jwt.verify(token, process.env.JWT_SECRET)",
      "jwt.check(token, process.env.JWT_SECRET)"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "La méthode `jwt.verify(token, secret)` est celle qui valide la signature du token en utilisant la clé secrète. Si la vérification échoue (mauvaise clé, token altéré), une erreur est levée."
  },
  {
    "questionText": "Quel statut HTTP est le plus approprié lorsqu'un token JWT a expiré ?",
    "options": [
      "400 Bad Request",
      "401 Unauthorized",
      "403 Forbidden",
      "419 Authentication Timeout"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Le code 401 Unauthorized indique que le client n'est pas authentifié. Bien que certains utilisent 403 (interdit), la spécification HTTP recommande 401 quand l'authentification a échoué ou est invalide, ce qui inclut l'expiration du token. Le 419 n'est pas standard."
  }
]