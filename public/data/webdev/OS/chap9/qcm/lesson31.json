[
  {
    "questionText": "Quelle est la première phase du processus de démarrage d'un ordinateur moderne, avant le chargeur d'amorçage (bootloader) ?",
    "options": [
      "Le chargement du noyau (kernel) en mémoire",
      "L'exécution de l'init ou systemd",
      "Le POST (Power-On Self Test) exécuté par le BIOS/UEFI",
      "Le montage du système de fichiers racine"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Le POST (Power-On Self Test) est la toute première étape effectuée par le BIOS ou l'UEFI pour vérifier que le matériel essentiel (CPU, RAM, etc.) fonctionne. Ensuite seulement le bootloader (GRUB, etc.) est chargé pour lancer le noyau."
  },
  {
    "questionText": "Sous Linux, quel est le rôle principal du noyau (kernel) lors de la phase de démarrage qui suit le bootloader ?",
    "options": [
      "Démarrer systemd ou init en tant que premier processus",
      "Monter le système de fichiers `/proc`",
      "Détecter et initialiser les périphériques matériels, puis monter le système de fichiers racine en lecture seule",
      "Lancer l'interface graphique (X11/Wayland)"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "Le noyau se charge d'initialiser les contrôleurs et périphériques, puis monte le système de fichiers racine (souvent en lecture seule pour des vérifications). Ensuite, il exécute le premier processus utilisateur (systemd ou init)."
  },
  {
    "questionText": "Sur une distribution Linux récente utilisant `systemd`, quel est le premier processus lancé par le noyau (PID 1) ?",
    "options": [
      "`/sbin/init` (SysV init)",
      "`/lib/systemd/systemd`",
      "`/bin/bash`",
      "`/usr/bin/kernel`"
    ],
    "correctAnswer": 1,
    "difficulty": "easy",
    "explanation": "Avec systemd, le PID 1 est `systemd`. C'est le premier processus lancé par le noyau après le démarrage ; il est responsable du lancement de tous les autres services et processus."
  },
  {
    "questionText": "Quelle commande systemd permet de vérifier l'état d'un service, par exemple `nginx` ?",
    "options": [
      "`systemctl status nginx`",
      "`systemd status nginx`",
      "`service nginx status` (uniquement pour SysV)",
      "`systemctl check nginx`"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "`systemctl status nginx` affiche si le service est actif (running), en échec, ou inactif, ainsi que les derniers logs associés."
  },
  {
    "questionText": "Sous systemd, quel type d'unité (unit) représente un démon/service géré par le système ?",
    "options": [
      "`.target`",
      "`.socket`",
      "`.service`",
      "`.device`"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "Les services (démons) sont définis par des fichiers `.service`. Les `.target` sont des groupes de services, les `.socket` pour l'activation via socket, et les `.device` pour les périphériques."
  },
  {
    "questionText": "Quelle commande permet de configurer un service `apache2` pour qu'il démarre automatiquement au boot sous systemd ?",
    "options": [
      "`systemctl start apache2`",
      "`systemctl enable apache2`",
      "`systemctl daemon-reload apache2`",
      "`systemctl reenable apache2`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "`systemctl enable` crée les liens symboliques dans les répertoires `multi-user.target.wants/` pour démarrer le service automatiquement au prochain démarrage. `systemctl start` le lance immédiatement mais sans activation automatique."
  },
  {
    "questionText": "Un enseignant vous demande de lister tous les services actifs (en cours d'exécution). Quelle commande `systemctl` est la plus adaptée ?",
    "options": [
      "`systemctl list-units --type=service --state=running`",
      "`systemctl list-services --active`",
      "`systemctl show --active`",
      "`systemctl --all --state=active`"
    ],
    "correctAnswer": 0,
    "difficulty": "medium",
    "explanation": "`systemctl list-units --type=service --state=running` affiche précisément les unités de type service ayant l'état 'running' (actif). L'option `--state=running` filtre correctement."
  },
  {
    "questionText": "Pendant le démarrage, `systemd` utilise des cibles (targets). Quelle est l'équivalent du niveau d'exécution (runlevel) 3 dans SysV init (multiuser en ligne de commande) ?",
    "options": [
      "`runlevel3.target`",
      "`graphical.target`",
      "`multi-user.target`",
      "`basic.target`"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "`multi-user.target` correspond au runlevel 3 : environnement multijoueur avec réseau mais sans interface graphique. `graphical.target` correspond au runlevel 5 (avec interface graphique)."
  },
  {
    "questionText": "Vous venez de modifier un fichier de configuration d'unité systemd (`/etc/systemd/system/mon.service`). Quelle commande devez-vous exécuter pour que systemd prenne en compte les changements ?",
    "options": [
      "`systemctl restart mon.service`",
      "`systemctl reload mon.service`",
      "`systemctl daemon-reload`",
      "`systemctl update mon.service`"
    ],
    "correctAnswer": 2,
    "difficulty": "medium",
    "explanation": "`systemctl daemon-reload` rechargement la configuration de systemd, re-lit tous les fichiers `.service`, `.target`, etc. C'est indispensable après toute modification d'un fichier d'unité. Ensuite seulement on peut utiliser `restart` ou `reload`."
  },
  {
    "questionText": "Sous systemd, que fait la commande `systemctl isolate multi-user.target` ?",
    "options": [
      "Elle liste les services actifs du target multi-user",
      "Elle démarre tous les services manquants pour atteindre multi-user.target et arrête ceux qui ne sont pas requis",
      "Elle redémarre le target multi-user",
      "Elle affiche les journaux du target multi-user"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "`isolate` change l'état du système en arrêtant tous les services qui ne sont pas nécessaires à la cible spécifiée et démarre ceux qui le sont. C'est l'équivalent d'un changement de runlevel."
  },
  {
    "questionText": "Sous systemd, quel mécanisme remplace avantageusement `cron` pour les tâches planifiées récurrentes avec journalisation intégrée ?",
    "options": [
      "`systemd.timer`",
      "`systemd.schedule`",
      "`systemd.cron`",
      "`systemd.job`"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "Les timers systemd (fichiers `.timer`) permettent de déclencher des services `.service` selon un calendrier (ex: quotidien, hebdomadaire), avec une intégration native dans les logs et un meilleur contrôle des dépendances que cron."
  },
  {
    "questionText": "Sur un système utilisant BIOS (non UEFI), où se trouve généralement le bootloader (GRUB) sur le disque ?",
    "options": [
      "Dans une partition EFI (ESP)",
      "Dans le Master Boot Record (MBR) et/ou la partition /boot",
      "Directement en mémoire flash du BIOS",
      "Dans la partition swap"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "En mode BIOS (legacy), GRUB est installé dans le premier secteur du disque (MBR) et des étapes suivantes dans la partition `/boot`. En UEFI, le bootloader réside sur la partition ESP (EFI System Partition)."
  },
  {
    "questionText": "Sous Linux, quel fichier contient les paramètres passés au noyau par le bootloader (comme `quiet splash` ou `nomodeset`) ?",
    "options": [
      "`/etc/default/grub`",
      "`/boot/grub/grub.cfg` (généré à partir de /etc/default/grub et /etc/grub.d/)",
      "`/etc/sysctl.conf`",
      "`/proc/cmdline` (pour voir ceux du démarrage en cours)"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "Les paramètres noyau sont définis par la variable `GRUB_CMDLINE_LINUX` dans `/etc/default/grub`. Puis `update-grub` génère `/boot/grub/grub.cfg`. Le fichier `/proc/cmdline` montre les paramètres utilisés par le noyau en cours d'exécution."
  }
]