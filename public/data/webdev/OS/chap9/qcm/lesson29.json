[
  {
    "questionText": "Quelle commande permet de créer un nouvel utilisateur nommé 'alice' avec son répertoire personnel par défaut sous Linux ?",
    "options": [
      "`useradd alice`",
      "`usernew alice`",
      "`adduser alice`",
      "`usermod -c alice`"
    ],
    "correctAnswer": 0,
    "difficulty": "easy",
    "explanation": "La commande `useradd alice` crée l'utilisateur 'alice'. Sur certaines distributions, `adduser` est un script Perl plus interactif, mais la commande standard de bas niveau est `useradd`. `usernew` n'existe pas et `usermod` sert à modifier un utilisateur existant."
  },
  {
    "questionText": "Dans quel fichier sont stockés les mots de passe des utilisateurs sous forme de hash (empreinte cryptée) sous Linux moderne ?",
    "options": [
      "`/etc/passwd`",
      "`/etc/login.defs`",
      "`/etc/shadow`",
      "`/etc/group`"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Le fichier `/etc/shadow` contient les mots de passe hashés, les dates de dernière modification, d'expiration, etc. Il est accessible uniquement par root. `/etc/passwd` contient les comptes publics (UID, GID, etc.) mais le mot de passe y est remplacé par 'x'."
  },
  {
    "questionText": "Vous voulez ajouter l'utilisateur 'bob' au groupe secondaire 'sudo' pour lui donner des droits d'administration. Quelle commande est correcte ?",
    "options": [
      "`groupadd bob sudo`",
      "`usermod -aG sudo bob`",
      ‎"`passwd -g sudo bob`",
      "`chown bob:sudo /home/bob`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "L'option `-aG` (append to Groups) permet d'ajouter l'utilisateur à un ou plusieurs groupes secondaires sans le retirer de ses groupes actuels. `usermod -aG sudo bob` ajoute bien bob au groupe sudo. `groupadd` crée un groupe, `passwd -g` n'existe pas et `chown` change le propriétaire d'un fichier."
  },
  {
    "questionText": "Quel est le rôle principal du fichier `/etc/group` ?",
    "options": [
      "Définir le mot de passe du groupe",
      "Stocker l'UID (User ID) de chaque groupe",
      "Lister les groupes du système et leurs membres",
      "Définir les quotas disque par groupe"
    ],
    "correctAnswer": 2,
    "difficulty": "easy",
    "explanation": "Le fichier `/etc/group` contient la liste des groupes, leur GID (Group ID) et les utilisateurs qui en sont membres (secondaires). Il permet de gérer les autorisations collectives."
  },
  {
    "questionText": "Quelle commande permet de supprimer définitivement l'utilisateur 'carol' ainsi que son répertoire personnel et son spool mail ?",
    "options": [
      "`userdel carol`",
      "`userdel -r carol`",
      "`deluser --remove-home carol`",
      "`groupdel carol`"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Sous Linux, `userdel -r` supprime l'utilisateur et supprime aussi son répertoire personnel (`/home/carol`) et son spool mail. `userdel` seul laisse ces répertoires. Sur Debian/Ubuntu, `deluser --remove-home` est l'équivalent mais la question est générale."
  },
  {
    "questionText": "Pour modifier le shell par défaut de l'utilisateur 'dave' en `/bin/zsh`, quelle commande est appropriée ?",
    "options": [
      "`chsh -s /bin/zsh dave`",
      "`usermod -s /bin/zsh dave`",
      "`shellconf dave /bin/zsh`",
      "`passwd -s /bin/zsh dave`"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "`chsh` (change shell) est l'outil dédié. `chsh -s /bin/zsh dave` change le shell de dave. L'utilisateur doit généralement connaître son mot de passe. `usermod -s` existe aussi, mais `chsh` est plus spécifique et disponible sans droits root si l'utilisateur agit sur son propre compte."
  },
  {
    "questionText": "Que signifie le caractère `x` dans le champ du mot de passe pour un utilisateur dans `/etc/passwd` ?",
    "options": [
      "Le compte est expiré",
      "Le mot de passe est stocké dans `/etc/shadow`",
      "L'utilisateur n'a pas de mot de passe",
      "Le mot de passe est chiffré avec l'algorithme X"
    ],
    "correctAnswer": 1,
    "difficulty": "medium",
    "explanation": "Le `x` indique que le mot de passe (hashé) se trouve dans `/etc/shadow`, qui contient les informations sensibles. C'est la configuration par défaut sur les systèmes modernes pour renforcer la sécurité."
  },
  {
    "questionText": "Vous devez créer un groupe 'projet' et y ajouter plusieurs utilisateurs. Quelle séquence de commandes est correcte ?",
    "options": [
      "`groupadd projet` puis `usermod -aG projet user1 user2`",
      "`addgroup projet` puis `gpasswd -M user1,user2 projet`",
      "`groupnew projet` puis `passwd -G projet user1 user2`",
      "`creategroup projet` puis `useradd -G projet user1 user2`"
    ],
    "correctAnswer": 1,
    "difficulty": "hard",
    "explanation": "`addgroup projet` (script Debian/Ubuntu) ou `groupadd projet` crée le groupe. Ensuite `gpasswd -M user1,user2 projet` définit la liste des membres. Une autre méthode correcte serait `groupadd projet` puis `usermod -aG projet user1` puis `usermod -aG projet user2`, mais l'option qui combine `gpasswd -M` est une seule commande valide."
  },
  {
    "questionText": "Quel fichier contient les règles de création par défaut des utilisateurs (UID par défaut, répertoire squelettique, etc.) ?",
    "options": [
      "`/etc/default/useradd`",
      "`/etc/login.defs`",
      "`/etc/adduser.conf`",
      "`/etc/user.conf`"
    ],
    "correctAnswer": 0,
    "difficulty": "very hard",
    "explanation": "Le fichier `/etc/default/useradd` contient les valeurs par défaut pour `useradd` (shell par défaut, répertoire parent des homes, répertoire squelette `/etc/skel`, etc.). `/etc/login.defs` contient d'autres paramètres (politique de mots de passe, UID min/max)."
  },
  {
    "questionText": "Quelle option de `usermod` permet de verrouiller le mot de passe d'un compte (en empêchant la connexion par mot de passe) ?",
    "options": [
      "`-L`",
      "`-U`",
      "`-l`",
      "`-p`"
    ],
    "correctAnswer": 0,
    "difficulty": "hard",
    "explanation": "`usermod -L utilisateur` verrouille (lock) le mot de passe en préfixant le hash par '!' dans `/etc/shadow`. L'option `-U` unlock. `-l` change le nom d'utilisateur (login). `-p` définit un mot de passe en clair (déconseillé)."
  }
]